---
id: D-012
title: "Language level: exactly CPython 3.14 in v1.0 (`python = \"3.14\"` in pycc.toml). No per-version grammar switches before v1.x; a later standard Python level requires a superseding ADR plus a machine-checked support/cumulative-fixture/oracle mapping. pycc-specific syntax remains forbidden"
status: proposed
---

# D-012

Index-only: no long-form entry recorded yet.

Language level: exactly CPython 3.14 in v1.0 (`python = "3.14"` in pycc.toml). No per-version grammar switches before v1.x; a later standard Python level requires a superseding ADR plus a machine-checked support/cumulative-fixture/oracle mapping. pycc-specific syntax remains forbidden
