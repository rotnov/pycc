# pycc Delivery Plan

How the roadmap in [ROADMAP.md](./ROADMAP.md) actually gets built: milestone decomposition, environment baseline, v0.1 execution strategy, and the autonomy policy that lets agents proceed without stopping for questions. Companion to [SPEC.md](./SPEC.md); written under D-013 (AI-first development).

## Program-level decomposition

Every ROADMAP.md milestone is its own sub-project: its own brainstorm → plan → implementation cycle, entered only when the previous milestone's acceptance criteria are green. v0.1, v0.2, and v0.3 are detailed below (see [Delivery mechanics](#delivery-mechanics) for why); v0.4+ get the same treatment when reached, since their design will be shaped by what v0.1–v0.4 teach us.

| Milestone | New crates / major additions | Depends on | Rough PR count |
|---|---|---|---|
| v0.1 hello, binary | `pycc`, `pycc_lexer`/`pycc_parser`/`pycc_ast` (vendored `ruff_python_parser`, D-003), `pycc_hir`, `pycc_types`, `pycc_mir`, `pycc_codegen`, `pycc_rt`, `pycc_diag`, `pycc_testkit`, CI matrix on 5 Tier-1 targets | — | ~7 (detailed below) |
| v0.2 collections & generics | `pycc_std` created (`math.sqrt`/`math.pi` only, D-136 — `sys` deferred, see row 14 below), recursive `Ty` (D-089) + monomorphization in `pycc_types`/`pycc_mir`, `--release`/LTO profile, `pycc.toml` | v0.1 | ~7 (detailed below) |
| v0.3 classes & pattern matching | class model, dataclasses, protocols, `match` w/ exhaustiveness, diagnostics registry grows | v0.2 | ~9 (detailed below) |
| v0.4 projects & incremental | multi-file/import resolution in `pycc_hir`, salsa-style incremental cache, `os`/`pathlib`/`json`/`datetime` | v0.3 | ~6-7 |
| v0.5 generators & ownership v1 | new crate `pycc_own` (escape analysis, move semantics, RC elision), generators as state machines | v0.4 | ~6-7 |
| v0.6 threads without GIL | Shareable/move checks in `pycc_own`, cycle collector, own parser replaces vendored `ruff_python_parser` (D-003 resolved) | v0.5 | ~6 |
| v0.7 transparent CPython interop | Standard `import` classification, pinned CPython/package/native-library bundling, `auto`/`allowlist`/`deny` policy enforcement, typed bridge and optional low-level `pycc.interop.cpython` API (D-128) | v0.6 | ~4-5 |
| v0.8 corpus at scale + bot | `corpus-bot` GitHub Action, `socket`/`http.client`/compression stdlib | v0.7 | ~4 |
| v0.9 async & packaging | `asyncio` subset on state machines, `--lib` C-ABI | v0.8 | ~4-5 |
| v1.0 spec freeze | PYTHON_STANDARDS Python 3.0–3.14 matrix complete, semantics deviations doc, benchmarks published, diagnostics/JSON frozen semver | v0.9 | ~3-4 |

## Environment baseline (verified, not assumed)

Verified empirically on the primary dev host (macOS, aarch64-apple-darwin) before committing to any of the below:

| Requirement | Finding | Consequence |
|---|---|---|
| Rust | `rustc 1.97.1` (stable, updated via `rustup update stable`; matches README's "1.97+" exactly) | Toolchain pinned via `rust-toolchain.toml` inside the repo — **not** a global `rustup default` change, since this machine has other toolchains (incl. a `solana` one) that must stay untouched |
| LLVM | `22.1.1` (single Homebrew keg; the `llvm@17`..`llvm@22` opt-paths are stale symlinks to the same keg, not distinct installs) | `inkwell = "0.9"` with feature `llvm22-1` — clean match, no version fudging needed |
| CPython oracle | `python3.14` → `3.14.7` in the repository/CI contract; verified locally from the official source release on 2026-08-15 because Homebrew still supplied 3.14.6 | Matches the unchanged v1 Python 3.14 language line and the current 3.14.7 patch target. |
| Local linker | Apple clang 21 / Xcode CLT `ld64` | Sufficient for the first vertical slice on native host; PR-3's `--target` work (D-026/D-028/D-031) routes through each host's own driver -- system `cc` (Apple clang) on macOS, bundled clang on Windows/Linux when `--target` is given -- not a universally bundled `lld` binary, which none of the three LLVM distributions this project installs actually ships |
| crates.io | Reachable (a bare `curl -I` 403s on crates.io's anti-bot filter — mundane, not a sandbox restriction; a real UA gets 200) | `cargo build` can fetch `ruff_python_parser`, `inkwell`, `rayon`, `mimalloc` |
| `gh` CLI | Authenticated, `repo`+`workflow` scopes | Can open PRs and push `.github/workflows` |
| `cargo-llvm-cov` | **Not** part of the rustup `llvm-tools` component — it is a separately distributed binary (own crate/release) that *uses* `llvm-tools-preview`'s `llvm-cov`/`llvm-profdata` at runtime. An earlier version of this plan conflated the two (caught by repo audit, issue #13); a spec that just says "install llvm-tools" fails in CI with "no such command: llvm-cov" | PR-1's CI skeleton installs both, explicitly and pinned: `rustup component add llvm-tools-preview` **and** a pinned `cargo-llvm-cov` install — never a bare "latest," per D-014's own no-unreviewed-drift spirit. CI smoke-checks the direct binary, then invokes it with the explicit `llvm-cov` subcommand under a clean unprivileged `nobody` environment whose workspace and trusted executables are read-only. A repository alias, `PATH` mutation, build script, or procedural macro therefore cannot replace the gate executable. |

Only macOS is locally verifiable. Linux x64/arm64 and Windows MSVC exist only via CI — so CI must be wired right after the first local slice works, not as a v0.1 finishing touch (see PR-3 below).

## v0.1 crate scope

Not every crate in [ARCHITECTURE.md](./ARCHITECTURE.md)'s workspace table is needed on day one. (The count is deliberately not restated here: it was stale, and the table is the single place it can be read off correctly.)

**Built now:** `pycc` (CLI/driver), `pycc_parser`/`pycc_ast` (thin wrapper over vendored `ruff_python_parser`), `pycc_hir`, `pycc_types` (strict public annotations plus monomorphic local/private-helper inference over the v0.1 primitive subset), `pycc_mir`, `pycc_codegen` (LLVM via `inkwell`), `pycc_rt` (minimal runtime), `pycc_diag`.

**Deliberately deferred:** `pycc_own` (ownership/escape analysis is a v0.5 item — semantics-preserving, perf-only, so v0.1 just uses RC/heap without ownership inference), `pycc_std` (real importable modules start v0.2; the Tier-0 builtins v0.1 needs — `print`, `range`, etc. — live as intrinsics directly in `pycc_rt`/`pycc_codegen`), `pycc_lexer` (D-017 — the vendored parser bundles lexing internally, nothing consumes a standalone token stream yet), `pycc_testkit` (D-018 — no PEP conformance matrix exists yet for a real harness to check against; `tests/slice0.rs` covers PR-2's two named fixtures ad hoc in the meantime).

## v0.1 execution strategy: thin vertical slice, then breadth

Three approaches were weighed: (A) thin end-to-end slice first, then grow features; (B) parallel crate teams integrated at the end; (C) fully lock every crate interface up front, then build sequentially.

**A was chosen.** B reproduces the "horizontal slicing" anti-pattern the repo's own `tdd` skill warns against — interfaces designed independently by separate workstreams diverge at integration time, and a compiler pipeline's crates have real, not incidental, sequential dependencies. C is excess upfront design for a project whose own TESTING.md philosophy is "prove it with a test," not "prove it on paper" — and the biggest unknown (does LLVM/inkwell/linking/CI actually work end to end on 5 platforms) is exactly the thing A de-risks first instead of discovering three weeks into frontend work.

1. **Slice 0**: `def main() -> None: print(42)` compiled through the entire pipeline to a running native binary on the local host. Proves crate wiring before anything else.
2. **CI matrix** on all 5 Tier-1 targets immediately after slice 0 — not at the end. From this point, CI is the source of truth for autonomous progress, not local macOS runs.
3. **TDD red-green, vertical slices** for feature growth: one conformance test per feature, written first, then the implementation — arithmetic → comparisons → `if`/`while`/`for`+`range` → functions/recursion → basic f-strings.
4. **Parallelism** only where genuinely independent (CI tooling, `pycc_diag` registry scaffolding, `docs/semantics.md`) via subagents; the pipeline core stays sequential because its dependencies are real.

### Rough PR breakdown

| PR | Content |
|---|---|
| 1 | Workspace scaffold (all crate stubs), `rust-toolchain.toml` (1.97.1), CLI skeleton, CI skeleton **with the coverage gate wired in from the first commit** (D-014): pinned `cargo-llvm-cov` installer (it is a separate binary, not part of `llvm-tools` — see the Environment baseline note below) + the `llvm-tools-preview` rustup component, `cargo llvm-cov --fail-under-lines 100 --fail-under-regions 100`. New ADR files added under docs/decisions/ (index regenerated): LLVM 22.1/inkwell 0.9 pin, vendored `ruff_python_parser` version |
| 2 | Slice 0: parser → HIR → types (passthrough) → MIR → LLVM codegen → link; "hello binary" runs locally. Covers **both** entry shapes named in ROADMAP.md's v0.1 scope: a `main()`-defining module, and a module with only top-level statements and no `main()` — module-level execution is a named conformance case here, not an incidental side effect of the `main()` path |
| 3 | CI matrix live on all 5 Tier-1 targets for slice 0, **plus one cross-compiled build**: `pycc build --target x86_64-apple-darwin` from the `macos-14` (arm64) runner, executed and verified on `macos-15-intel` (x64) — same-OS/cross-arch, per D-026, since true cross-OS needs a target sysroot this project doesn't bundle yet (verified empirically, not assumed). Cross-compilation is a v0.1 requirement from D-011/README/ARCHITECTURE/CLI_SPEC, not a nice-to-have — it does not get to hide behind "CI matrix live" meaning same-host-per-target builds only |
| 4 | Frontend depth: full v0.1 grammar, real T0001 + local inference, first diagnostic codes with snapshot tests. First per-PR frontend benchmark baseline recorded here (see Performance gate below) since this is the first PR with a non-trivial frontend to measure |
| 5 | Codegen depth: full v0.1 feature set (int/float/str/bool, arithmetic, control flow, recursion, f-strings); runtime fleshed out (overflow→bigint per D-001, small-string opt per D-007). `--debug` profile only — `--release`/LTO is a v0.2 item (see Testing scope below), not built here |
| 6 | Conformance + benchmark gate: fib + mandelbrot-ascii vs. the current pinned CPython 3.14.7 oracle on all 5 targets, `--debug` profile, as a plain `tests/conformance.rs` integration test rather than a new `pycc_testkit` crate (D-085); `pycc check` benchmark <75ms/1k LOC; diagnostic output matches CLI_SPEC.md's example byte-for-byte |
| 7 | Buffer: close whatever's left so all v0.1 ROADMAP.md acceptance bullets are simultaneously green |

Each row above absorbs one gap an automated repo audit found in the original version of this plan (tracked as GitHub issues #9–#13): the cross-compilation gap (#9, → PR-3), the debug/release conformance contradiction (#10, → PR-5/PR-6 and Testing scope below), the missing module-level-execution case (#11, → PR-2), the missing early performance gate (#12, → PR-4 and Performance gate below), and the `cargo-llvm-cov` installation error (#13, → PR-1 and Environment baseline below). Issue #14 (measure platform-specific code per-platform rather than exempting it) is deferred — there is no platform-specific code yet for the question to apply to; it rides along whenever D-014 next gets touched.

### Performance gate (resolves #12)

ARCHITECTURE.md requires benchmarks in CI on every PR with a >2% frontend-regression merge block. This was originally scheduled to start immediately once PR-4 made the frontend non-trivially executable, deliberately not deferred to a single check in PR-6 (D-042/D-044): every subsequent PR would record a `pycc check` timing run and fail if it regressed >2% against the latest baseline published by a successful `main` run, with pull-request code running only in `frontend-perf-measure` and the separate hash-verified `frontend-perf-gate` consuming its Criterion JSON as untrusted data, restoring the fresh main-only cache namespace but never publishing to it so repeated heads cannot ratchet or shadow the canonical comparison (D-046).

**D-047 was a temporary deferral, superseded by D-048 after PR-4 merged.**
D-048's exact-successful-main artifact gate established the required isolated
boundary and retired the earlier cache transport. D-051/D-053 now supersede
only its cross-run timing transport: every run measures the exact predecessor
and candidate sequentially on one hosted runner, seals the predecessor timing
before candidate code runs, and applies the unchanged greater-than-2% median
regression block through the predecessor-owned hash-verified comparator. The
D-048 activation variable, bootstrap branches, digest, and fixture are absent;
D-048 through D-050 remain historical rationale rather than live configuration.
PR-5 may proceed with the performance invariant already active.
This remains deliberately lightweight and distinct from the full
pyperformance/Nuitka/Codon/mypyc comparison suite (TESTING.md Layer 7), which
stays out of scope until v0.2.

D-051 records, and D-053 corrects, the paired transport after repeated docs-only
CI runs proved that absolute estimates from two different hosted runners can
exceed the 2% threshold despite narrow within-run confidence intervals. The
exact predecessor and candidate are measured on one runner, and their paired
estimates are passed to a dedicated hash-verified median comparator inside the
same isolated review boundary. The active workflow binds the benchmark sources, root and
workspace manifests, local build scripts, lockfile, Rust toolchain, and Cargo
configuration; it seals the predecessor artifact before candidate code runs,
binds both downloads to the distinct artifact IDs returned by their trusted
upload steps, flattens each single-ID download into its own exact destination,
and requires any contract drift to use its own reviewed transition. D-056 keeps
this transport, threshold, required `ci-gate` fan-in, and benchmark unchanged.

D-056 introduced the executable-input identity rule for residual same-runner
order variance. Main run 30198852753 failed at `+3.14%` even though `src/`,
`crates/`, and every
already-bound benchmark/build input were unchanged; run 30199477003 later
passed the same unchanged-input class at `+0.86%`. The active workflow
therefore retains both timings as telemetry but makes them non-blocking only
when a pre-execution trusted comparison proves the complete executable inputs
identical. Any `src/` or `crates/` difference still enters the unchanged
greater-than-2% median comparison. The historical D-056 workflow first
activated that rule. D-062 responds to the later identical-source-pair
`+0.10%`/`+3.66%` contradiction that D-056's changed-input path intentionally
does not remove. The active source-aware five-replicate successor uses:
median-of-five per revision when inputs changed, D-056's non-blocking telemetry
when they are identical, all ten JSON files retained, predecessor samples
sealed before candidate execution, and no result-dependent retries. The
checker authorizes the exact active D-112 whole workflow (`frontend-perf-measure`/
`frontend-perf-gate` moved from `macos-14` to `ubuntu-latest`, confirmed by
five real shadow-measurement runs before activation), which is byte-identical
to its reviewed fixture; its performance-job content keeps D-062's comparator
logic unchanged, though the runner and LLVM install step now differ from the
D-062 fixture. D-100 (composing D-091's release-runtime/manifest-relaxation
changes with D-099's Windows vcpkg cache) was the prior active digest and, like
D-056 and D-062, remains historical audit evidence, coexisting in the checker
allowlist pending its own retirement round. The `ci-gate` fan-in,
benchmark contract, artifact-ID binding, and isolated predecessor-owned
comparison boundary remain unchanged. **Update (2026-08-03, D-114):** the
2% threshold itself is no longer current — it was raised to 7.0% via a
corrected six-round D-103 propose/activate sequence, to accommodate v0.2
PR-10's real, one-time `Ty`-migration cost (D-109), not runner noise.
`REVIEWED_PERF_CI_WORKFLOW_SHA256S` now coexists `[D100, D112, D114]`;
`.github/workflows/ci.yml` matches D-114's shape. Issue #296 tracks
lowering the threshold back toward 2.0% once this one-time cost is
absorbed into every future baseline.

## Autonomy policy ("no questions" mechanics)

**Spec is law.** Where an existing doc already decided something, follow it literally. Where the spec is genuinely silent (as with the exact LLVM/inkwell version above), the agent picks the most conservative option that is *actually available in this environment* — never a hypothetical — records it as a new dated `docs/decisions/D-NNN-<slug>.md` file using [its existing template](./decisions/TEMPLATE.md) and regenerates the [generated index](./decisions/README.md), and continues without stopping.

This resolves the one real tension with the merged `tdd` skill, which asks to confirm test seams with the user before writing tests: the approved plan (this document + the writing-plans output that follows it) enumerates the seams per crate up front. Approval of the plan **is** the seam confirmation; implementation agents do not re-ask.

This policy governs architectural/implementation forks only. Standing safety rules (force-push, branch deletion, rewriting published history, etc.) are unaffected and still pause for explicit confirmation.

## Delivery mechanics

This plan and its revisions are committed to their own short-lived branch off `main` and opened as a docs-only PR, reviewed before merge like any other change — review weight matched to risk, a read-through for docs rather than the full multi-agent pipeline reserved for compiler code. Every subsequent PR in the table above is its own feature branch off `main`, merged only once CI is green on all Tier-1 targets and that PR's slice of the v0.1 acceptance criteria is demonstrably met.

## Testing scope for v0.1

Of TESTING.md's 7 layers: Layer 1 (per-crate unit tests) from the start, Layer 3 (diagnostic snapshot tests) as soon as `pycc_diag` exists, Layer 5 (runtime property tests) in minimal form for the v0.1 runtime subset. **Out of scope for PR-1/PR-2**: Layer 2 (the real `pycc_testkit` conformance harness — deferred per D-018 to PR-4/PR-6, once there's a PEP matrix for it to check against; `tests/slice0.rs` covers the two named slice-0 fixtures ad hoc until then), Layer 4 (differential fuzzing), Layer 7's full cross-compiler comparison suite (vs. Nuitka/Codon/mypyc) — these remain out of scope through v0.1. Layer 6 (OSS corpus) does **not** start at v0.2 as originally planned here: D-088 found the literal Tier-1 OSS packages (`tomli`/`packaging`/`more-itertools`) need stdlib breadth v0.2 doesn't have, and substitutes a hand-authored container/generics corpus for v0.2's own acceptance gate instead (see the v0.2 section below); real Layer 6 OSS corpus work starts whenever `PYTHON_STANDARDS.md`'s corpus table is re-verified against actual import surfaces and reassigned to a real milestone (tracked in #183), not automatically at v0.2. The lightweight per-PR frontend timing check is a different, narrower thing that *does* start in v0.1 — see Performance gate above (resolves #12).

**Debug/release conformance (resolves #10):** TESTING.md's conformance-harness rule ("compile `--debug` and `--release` both... flips to ✅ only when green on all Tier-1 targets in both profiles") describes the steady-state contract once `--release` exists. It does not apply yet: `--release`/LTO is a named v0.2 item (see the milestone table above), so for the whole of v0.1 the conformance harness runs `--debug` only, and no v0.1 PEP/feature is held to a `--release` bar that has nothing to build against. TESTING.md's wording is annotated accordingly rather than left to look like a v0.1 requirement no PR could actually satisfy.

Cutting across all of these: the D-014 coverage gate (`cargo llvm-cov --fail-under-lines 100 --fail-under-regions 100`) applies to every crate from PR-1 on — it is not a v0.1-specific item but a standing requirement, wired into the CI skeleton before any crate has a chance to accumulate untested code. Each task in the implementation plan below writes its test alongside its code for exactly this reason.

## Scope honesty

v0.1 is realistically a multi-PR, multi-session effort — real Rust/LLVM compilation and 5-target CI have minutes-scale iteration loops, not seconds. Work proceeds PR by PR, autonomously, opening each PR as it's ready, without stopping for questions, for as long as a session productively runs.

## v0.2 execution strategy: perf/tooling foundation, then the riskiest new capability, then breadth

Full brainstorm output (dialogue conducted with the `advisor` tool per the standing autopilot goal, since the user is unavailable until v0.2 ships): [`docs/superpowers/specs/2026-07-28-v0-2-collections-generics-design.md`](./superpowers/specs/2026-07-28-v0-2-collections-generics-design.md). Acceptance criteria as corrected by [D-088](./decisions/D-088-correct-v0-2-s-acceptance-criteria-before-any-v0.md); the `Ty` representation change every later PR depends on is [D-089](./decisions/D-089-ty-becomes-a-recursive-heap-boxed-enum-for-v0-2.md).

Same three-approach weighing as v0.1 (thin slice first, vs. parallel crate teams, vs. full upfront interface lock) applies again, with the same conclusion (A) for the same reasons: `pycc_types`/`pycc_mir`/`pycc_codegen`/`pycc_rt` have real sequential dependencies for monomorphization, and the biggest unknown (does compiling one real generic container end-to-end actually work) should be proven before building breadth on top of it.

Two corrections to v0.1's ordering pattern, both from D-088's verification findings:

1. **`--release`/LTO lands first, not last.** `nbody ≥ 20× CPython` (ROADMAP.md's own v0.2 accept bullet) is unreachable in a `--debug` build — v0.1's codegen never enabled LLVM optimization passes or RC-elision-adjacent tuning. The existing `frontend-perf-*` CI jobs measure `pycc check`'s own speed (parse+lower+check), not generated-code runtime speed — an nbody benchmark harness comparing the compiled binary against CPython is new infrastructure nobody has built. Landing this first means every later PR benefits from a working `--release` profile for its own tests, and the perf-critical acceptance bullet is instrumented from day one instead of discovered unmeasurable at the end.
2. **The real per-PEP conformance harness lands before monomorphization, not as a closing buffer.** D-088 revised the target to ≥15 conformance-matrix rows; that number cannot be measured without fixtures under `tests/conformance/pyXY/` and a runner comparing them to the pinned CPython oracle (`PYTHON_STANDARDS.md`'s documented but never-built structure — D-018/D-085 deferred `pycc_testkit` specifically "until there's a PEP matrix to check against," which now exists). Building this early means every subsequent PR can add its own fixture and watch the count climb, rather than reaching PR-14 with no way to prove the milestone's gate.

### Rough PR breakdown

| PR | Content |
|---|---|
| 8 | `--release`/LTO profile (already specified in `docs/CLI_SPEC.md`) + `pycc.toml` (schema already specified there too) + the nbody benchmark harness (measurement contract in the design doc §1: hand-adapted, fully-unrolled, scalar-only 5-body simulation — no container dependency, since PR-10/11 haven't shipped `list`/`tuple` yet — same-machine paired median-of-5 comparison, `--release` pycc vs. the current pinned CPython 3.14.7 oracle). D-126 Phase A records both wall-clock and exact-child CPU-time medians while the ratio ≥ 20 gate remains on wall clock for 2 of 5 Tier-1 targets, with 12x on macOS aarch64, 15x on `windows-latest`, and 18x on `ubuntu-24.04-arm` per D-095/D-096/D-101's own documented, measurement-backed exceptions. D-129 Phase B evaluates five observations per leg and keeps that wall-clock methodology and those floors: CPU-time variance is effectively unchanged on the motivating Ubuntu x86_64 leg and 44.2% worse on Windows, so CPU time remains diagnostic telemetry rather than a gate. Lowest architectural risk of the milestone — CLI driver + `pycc_codegen`'s LLVM optimization-level wiring, no type-system changes — and unblocks the perf-critical acceptance bullet immediately |
| 9 | **Delivered.** Real per-PEP conformance harness seeded with 8 no-new-work PEPs (238, 3105, 3107, 3131, 414, 484, 498, 515) from the design doc §2 (already true of v0.1's shipped surface, empirically verified — an earlier draft of this row also claimed PEP 526 and PEP 594 needed no new work, which a review caught was false for both, and PEP 649/749 was later found to be a third wrong "already true" claim, not reachable until v0.3's class model exists; see the design doc §2's corrected table and its Update note) plus PEP 526's own real `Stmt::AnnAssign`/`HirStmt::AnnAssign`/`T0025`/`MirStmt::NoOp` work end to end. D-102 recorded the architecture decision: extend the existing `tests/conformance.rs` in place rather than finally building `pycc_testkit` (D-018/D-037/D-085's third deferral) — the resulting 11-fixture matrix still didn't justify a new crate. All 9 new fixtures pass byte-for-byte against pinned CPython 3.14.6 in both `--debug`/`--release` on all 5 Tier-1 targets. |
| 10 | **Merged to `main`** ([PR #236](https://github.com/rotnov/pycc/pull/236), merge commit `48f793e`, 2026-08-03). `Ty` representation migration (D-089: recursive `Box`/`Vec`-based variants, `Copy` dropped for `Clone`; D-089's own pre-migration estimate was ~729 call sites, later re-verified at 857 -- hir 36, types 335, mir 111, codegen 372, rt 3, D-105's own breakdown) as its first task, then monomorphization foundation + a `list[int]` end-to-end thin slice — literal construction, `.append()`, indexed read, `len()`, and `for`-iteration, all through the real `pycc build`/`run` CLI (D-105 through D-108: the five scope cuts, `PyIntListObj`'s raw-untagged runtime representation, `pycc_codegen::Scalar::List`, and the negative-index rejection) — plus its own PEP-585 (`list[int]`) conformance fixture and dual-profile test, verified locally byte-for-byte against the pinned CPython 3.14.6 oracle and, later, observed passing in CI on all 5 Tier-1 targets ([run 30608030517](https://github.com/rotnov/pycc/actions/runs/30608030517)). v0.2's "slice 0" — proves the whole monomorphization pipeline works before building breadth on it. `PYTHON_STANDARDS.md`'s PEP 585 row is flipped to `✅` and the v0.2 conformance count moved 9→10 on that CI evidence (see `ROADMAP.md`'s v0.2 section). A self-inflicted `frontend-perf-gate` regression this migration caused (D-109: `Ty`'s size growing from 1 to 24 bytes, then boxed down to 16 bytes, Task 14) was real and architectural, not hosted-runner noise (confirmed non-noise once D-112 moved this gate to `ubuntu-latest`); it merge-blocked this PR until D-114 (`docs/decisions/D-114-raise-frontend-perf-gate-s-threshold-to-7-0-for.md`) raised the gate's threshold from 2.0% to 7.0% via a corrected six-round D-103 sequence, landing on `main` ([PR #304](https://github.com/rotnov/pycc/pull/304), 2026-08-03) with no emergency bypass. Once D-114 activated, this PR merged its own rebase onto that `main` tip and passed `frontend-perf-gate` at its measured `4.7409%` regression, comfortably under the new 7.0% threshold. `list[T]` refcounting stays leak-only by design (D-107); `dict`/`set`/`tuple` generics were PR-11's own scope (now implemented, see row 11 below) |
| 11 | **Merged to `main`** ([PR #305](https://github.com/rotnov/pycc/pull/305), squash merge commit `9a7a6db`, 2026-08-03): `dict[str, int]` + `set[int]` (PR-11a) + `tuple[...]` (PR-11b), reusing PR-10's monomorphization machinery. PR-11a (`dict`/`set`): `set[T]`'s own representation question (design doc §3: swiss table without a value slot, non-guaranteed ordering matching CPython, unlike `dict`) is resolved by D-121: v0.2 ships *neither* container as a real hash table — both are a dense, insertion-ordered array with linear-scan lookup instead (a real hash table remains the v1.0 target, not silently abandoned). D-122 scopes each container to exactly one key/element combination reaching codegen (`dict[str, int]`, `set[int]`; `T0036`/`T0038` reject every other combination, mirroring `list[int]`'s own `T0034`). D-123 scopes each container's own operations: `dict[str, int]` ships `d[k] = v` insert-or-update (new key appended, existing key updated in place) alongside `d[k]` read, `len(d)`, and `for k in d:`; `set[int]` ships literal construction with dedup, `len(s)`, and `for x in s:`, but no `.add()` and no membership test (`in` parses fine -- the parser produces a valid `CmpOp::In` node like any other comparison operator -- but `pycc_hir`'s lowering step rejects it with the same generic `C0001` diagnostic used for `is`/`is not`/chained comparisons, so it has no HIR/type-checker/codegen support anywhere in this compiler yet). D-124 keeps both leak-only, matching `list[int]` (D-107). Adds the dict-insertion-order conformance fixture (`dict_order.py`, proving insertion order survives mutation, not just literal order) plus a minimal order-independent `set[int]` PEP-585 fixture (`pep_0585_set_int.py` — set iteration order is not asserted byte-for-byte against CPython, per D-123). PR-11b (`tuple[...]`): structurally different from the other three containers — every v0.2-accepted element type (`int`/`bool`/`float`, any mix, any arity ≥ 1) is a fixed-width scalar with a compile-time-known count, so D-115 represents a tuple as an LLVM struct held by value (an SSA aggregate; no heap object, no refcounting question). D-116 scopes element types (`T0039`) and requires a literal, non-negative, in-range integer index for `t[k]` (`T0040`, stricter than `list[int]`'s runtime-checked index, since a heterogeneous tuple's element type at position `k` is only knowable at compile time). Adds `tuple_heterogeneous.py` (heterogeneous literal construction, literal-index reads across all 3 element types, module-global and function-local storage, an inline unnamed-tuple subscript). All three new fixtures (`dict_order.py`, `pep_0585_set_int.py`, `tuple_heterogeneous.py`) pass byte-for-byte against the pinned CPython 3.14.6 oracle in both `--debug` and `--release`, verified locally, and now observed passing in CI on all 5 Tier-1 targets ([PR #305](https://github.com/rotnov/pycc/pull/305)) — `PYTHON_STANDARDS.md`'s dict-insertion-order row is flipped to `✅` on that evidence, and the shared PEP 585 row's prose reflects `dict`/`set`/`tuple` alike. **Not delivered by either sub-PR, tracked as `docs/ROADMAP.md` follow-ups:** `set[int]`'s membership test (`in`, D-123); leak-only refcounting for all of `list`/`dict`/`set` (D-107/D-124); `tuple[...]`'s own `for x in t:` iteration, unpacking assignment, and annotation syntax (D-116 point 3); and, discovered during PR-11b's own docs sweep, a pre-existing gap shared by all four container types — an entirely unannotated private helper cannot have a container-typed parameter or return type inferred from real source, because `pycc_types`' signature-inference solver is scalar-only by construction (D-116 point 4's correction note) |
| 12 | **Merged to `main`** ([PR #305](https://github.com/rotnov/pycc/pull/305), squash merge commit `9a7a6db`, 2026-08-03, combined with PR-11 in the same PR): list/dict/set comprehensions, `list[int]` slicing, `list.pop()`/`dict.get()`/`set.add()`, and the PEP-709 conformance fixture, reusing PR-10/PR-11's monomorphization machinery. D-117 scopes comprehensions to `Stmt::Assign`-RHS position only, reusing each container's existing iterable polymorphism (`range()`/list/dict/set sources) and, critically, the *same construction primitives* their literal forms already call (`pycc_rt_int_list_new`/`pycc_rt_dict_new`/`pycc_rt_int_set_new` plus the existing per-element append/insert/add helpers) — no new `pycc_rt` allocation function was needed. A comprehension's own loop variable is rewritten to a synthesized, collision-proof internal name so it cannot leak into or clobber an enclosing same-named binding, the one CPython-observable guarantee PEP 709 depends on that this AOT compiler (no bytecode, no frames) can actually test. D-118 ships `list[int]` slicing (`xs[a:b:c]`) with non-negative clamped bounds and a positive step, extending D-108's honest-panic convention from indexing to slicing; `dict`/`set` correctly stay unsupported for slicing (real Python raises `TypeError` there too, not a scope cut), `tuple[...]` slicing is a genuine deferral. D-119 ships exactly one new growable/lookup operation per mutable container type — `list.pop()` (no-argument, panics on empty), `dict.get(key, default)` (two-argument form only, no `None`-returning zero/one-argument form), `set.add(value)` (a second call site for the already-existing `pycc_rt_int_set_add`) — reusing existing `T0021`/`T0033` diagnostics rather than minting new `T0xxx` codes; `tuple[...]` gets no new method (real Python tuples have none worth adding), `set[int]`'s membership test (`in`) stays deferred (re-affirms D-123). D-120 records why the PEP-709 fixture (`tests/fixtures/pep_0709_comp_inline.py`, `pep_0709_comp_inline_matches_cpython_3_14_6_byte_for_byte`) demonstrates loop-variable non-leakage rather than literal bytecode-inlining, which has no analog in pycc's architecture. A second fixture, `tests/fixtures/container_methods_slicing.py` (`container_methods_slicing_matches_cpython_3_14_6_byte_for_byte`), is a single breadth fixture covering slicing, all three new methods, and both comprehension source-container combinations the PEP-709 fixture doesn't already exercise — both container element types are always printed element-wise (`for v in xs: print(v)`), never the container itself, since `to_str`/`truthy` stay unimplemented for every container (D-107/D-124/D-116, reaffirmed by D-120). Both fixtures pass byte-for-byte against the pinned CPython 3.14.6 oracle in both `--debug` and `--release`, verified locally, and now observed passing in CI on all 5 Tier-1 targets ([PR #305](https://github.com/rotnov/pycc/pull/305)) — `PYTHON_STANDARDS.md`'s PEP 709 row is flipped to `✅` on that evidence, mirroring PR-11a's dict-order row's own identical discipline. **Not delivered by this PR, tracked as `docs/ROADMAP.md` follow-ups:** comprehensions used outside `Stmt::Assign`-RHS position (as a function argument, return value, or nested sub-expression, D-117); `tuple[...]` slicing (D-118); `set[int]`'s membership test (D-123/D-119, unaffected by this PR's `.add()` addition); real (non-leak-only) refcounting for any container (D-107/D-124, unaffected by this PR); and a project-wide gap re-confirmed while verifying this PR's own fixtures against the real CPython oracle, already recorded twice before this PR (`tests/fixtures/nbody.py`'s PR-8 comment; `tests/slice1_codegen_depth.rs`'s own Task 9 comment, this same plan's commit `e92adec`) — this compiler has no unary-minus support anywhere (`pycc_hir`'s `lower_expr` has no match arm for `Expr::UnaryOp` at all, so any negated/negative literal falls through to the generic `C0001` diagnostic), a materially broader gap than D-108/D-118's own deliberate, narrower "no negative index/slice-bound" scope cuts |
| 13 | **Implementation complete on branch `feat/v0-2-pr13-generics-type-statement`, not yet merged to `main`** (as of 2026-08-04). `TODO(PR-13 Task 7): fill in PR link, squash-merge commit, and merge date once this branch's PR actually merges, mirroring rows 10/11/12's exact style above.` PEP 695 generic functions (`def f[T](x: T) -> T`, D-133/D-134: `Ty::Param(Box<String>)` substituted per call site, exactly one type parameter, scalar-only instantiation, `T0042` diagnostic for every rejected shape) and the `type` statement (D-088 scope: not generic classes — v0.3's class model doesn't exist) + legacy `TypeAlias` (D-135: both are compile-time-only name-to-`Ty` substitution inside `pycc_types`/`pycc_hir`, no `HirItem`/`MirItem`, no runtime representation, sharing `T0042` for a rejected generic alias) ship real codegen for the one-type-parameter/scalar-only/non-generic-alias thin slice described above, reusing PR-10's monomorphization machinery exactly as D-134 anticipated. Adds `pep_0695_generics.py` (a one-type-parameter generic function called at 3 sites across `int` and `str`) and `pep_0613_typealias.py` (a legacy `X: TypeAlias = int` alias used as a parameter/return annotation) — both verified locally byte-for-byte against the pinned CPython 3.14.6 oracle in both `--debug` and `--release`; `PYTHON_STANDARDS.md`'s PEP 695/613 rows stay `☐` until this branch's own CI is observed green on all 5 Tier-1 targets in both profiles, per D-102's "never speculatively flip" policy — that flip is deferred to this same Task 7 merge step, not done in this docs sweep. **Not delivered by this PR, tracked as `docs/ROADMAP.md` follow-ups:** multiple type parameters, a type parameter in a container position (`def f[T](x: list[T])`), generic aliases (`type Alias[T] = list[T]`), PEP 696 defaults, and general recursive generic instantiation (a generic function calling itself or another generic function is rejected pre-codegen with `T0042` rather than supported, added during this PR's final review) |
| 14 | **Implementation complete on branch `feat/v0-2-pr14-stdlib-imports`, not yet merged to `main`** (as of 2026-08-04). `TODO(PR-14 Task 7): fill in PR link, squash-merge commit, and merge date once this branch's PR actually merges, mirroring rows 10-13's exact style above.` New workspace crate `pycc_std` (D-136: dependency-free plain-data registry) ships exactly `math.sqrt`/`math.pi` — narrower than D-136's own original point-2 symbol list (`math.floor`/`pow`/`e`, `sys.exit`), because those are the only two symbols with a concrete Task 4 codegen lowering landing in this PR; `sys` is not shipped at all (no `StdModule::Sys` variant), deferred pending a future PR that builds either `NoReturn`-shaped divergence handling or a `list[str]`-from-argv construction path (D-136 addendum in `docs/decisions/D-136-pycc-std-is-a-plain-data-crate-math-sys-symbols.md`). `pycc_hir` gains `Stmt::Import`/`Stmt::ImportFrom` lowering (D-137: module-level side-table, `C0001` for every unsupported shape, new `C0002` for a recognized module with an unregistered symbol) and resolves `math.sqrt(x)`/`math.pi` textually at lowering time into `HirExpr::Call{callee: "math.sqrt", ..}`/`HirExpr::Name("math.pi")` — a documented, non-flow-sensitive scope trim (real "was `math` actually imported" verification is not attempted, precedented by this same file's existing `X: TypeAlias` bare-name acceptance) rather than threading the import side-table through every recursive expression-lowering call site. `pycc_types` binds both through both solver passes (mirroring the existing `print`/`len`/`float` hand-recognized-intrinsic pattern, reusing `T0021` for every arity/type-mismatch diagnostic). `pycc_mir`/`pycc_codegen` lower `math.sqrt` to a real libm `sqrt` call (confirmed before implementing that no new linker wiring was needed — `main.rs` already links `-lm` on Linux per D-028, macOS/Windows fold libm in implicitly) and `math.pi` to a compile-time `f64` immediate, no runtime call. Adds the D-138 PEP-594 fixture pair (`pep_0594_dead_battery.py` proving import resolution genuinely works, `pep_0594_dead_battery_rejected.py` proving `import cgi` still cleanly `C0001`s) and the D-139 hand-authored container/generics corpus (`v0_2_container_generics_corpus.py`: list/dict/set/tuple literals, a list and dict comprehension, list slicing, `.pop()`/`.get()`/`.add()`, a generic function called on `int` and `str` — using a `tuple[int, int]` literal rather than D-139's own illustrative `tuple[int, str]` text, since `str` tuple elements are still `T0039`-rejected per D-116, an addendum recorded on D-139). All three new fixtures verified locally byte-for-byte against the pinned CPython 3.14.6 oracle in both `--debug` and `--release`; `PYTHON_STANDARDS.md`'s PEP 594 row and the container/generics corpus's `ROADMAP.md` acceptance bullet stay unflipped/unchecked until this branch's own CI is observed green on all 5 Tier-1 targets in both profiles, per D-102's "never speculatively flip" policy — deferred to this same Task 7 merge step. Full workspace `cargo test --workspace` passes and `cargo llvm-cov --workspace --fail-under-lines 100 --fail-under-regions 100` is genuinely 100% across every crate as of the last commit on this branch before the PR was opened. |

Each PR after PR-9 adds its own conformance fixture(s) to the PR-9 harness as its feature lands, so the ≥15-row count is verified incrementally rather than audited once at the end.

## v0.3 execution strategy: class model foundation, then breadth and the highest-risk item last

Full brainstorm output (dialogue conducted with the `advisor` tool per the standing autopilot goal, since the user is unavailable mid-autopilot per [D-127](./decisions/D-127-autonomous-agent-operation-model.md)): [`docs/superpowers/specs/2026-08-06-v0-3-classes-pattern-matching-design.md`](./superpowers/specs/2026-08-06-v0-3-classes-pattern-matching-design.md). Acceptance criteria as corrected by [D-153](./decisions/D-153-correct-v0-3-s-conformance-target-before-any-v0.md) (the ≥45-PEP bullet was unverified against `PYTHON_STANDARDS.md`'s actual matrix, the same root cause D-088 already fixed once for v0.2).

Same "thin slice first" conclusion as v0.1/v0.2, for the same reason: `pycc_hir` has no class-definition representation and no class-instance `Ty` variant at all today, so PR-15 (class model foundation) is a hard prerequisite for PR-16 through PR-22 — there is nothing for inheritance, `@property`, dataclasses, enums, protocols, or exception classes to build on until it lands.

### Sequencing rationale: hard dependencies vs. recommended sequence

Unlike v0.1/v0.2's single dependent chain, v0.3 has real parallel tracks once PR-15 lands, so this table separates a **hard depends-on** (the PR/decision literally required for the code to compile/typecheck/pass tests) from a **recommended sequence** note where the two differ — mirroring v0.2's own "Two corrections to v0.1's ordering pattern" subsection, so later PRs aren't read as serially blocked when several are actually independent:

| PR | Hard depends-on | Sequencing note |
|---|---|---|
| 15 | v0.2 (done) | Foundational; everything below depends on it. |
| 16 | PR-15 | Sequenced immediately after PR-15 on its own merits — dispatch/MRO decisions here are architecturally central (property override, protocol conformance via inherited methods, dataclass inheritance, and enum's inherit-from-`Enum` special case all interact with whatever design lands here). PR-17..20 do *not* hard-depend on PR-16 and could be reordered ahead of it by an implementer with parallel capacity. |
| 17 | PR-15 | Does not hard-depend on PR-16, but property override in a subclass is a common real-world idiom worth testing once PR-16 lands — sequenced after PR-16 for fixture richness, not compile-time necessity. |
| 18 | PR-15 | Does not hard-depend on PR-16/17, but dataclass inheritance and dataclasses with property-backed fields are realistic conformance fixtures — sequenced after PR-16/17 for the same reason as PR-17. |
| 19 | PR-15 (hard); PR-16 (recommended, for the inherit-from-`Enum` special case to reuse real inheritance machinery) | — |
| 20 | PR-15 | Sequenced after PR-16 so the general dispatch design (if PR-16's open polymorphism question resolves toward needing one) is available to build on, not because protocols themselves need inheritance. |
| 21 | PR-15 (hard, class patterns only — non-class patterns have no class-model dependency) | Could start in parallel with PR-16..20 for non-class-pattern work; class-pattern support specifically should land after PR-15. #359 landing is recommended-early (arm-binding correctness inside solver-inferred private helpers), not blocking — see the design doc's Context section for the corrected, narrower relationship. |
| 22 | PR-15 (hard, custom exception classes); PR-16 (hard, for subclassing); **D-005 resolution (hard, blocks all code in this PR)** | Sequenced last: highest uncertainty (D-005 unresolved, native-unwinding mechanism not chosen — see the design doc §2), and custom exception classes need PR-16's inheritance machinery regardless. Starting D-005's cross-platform research early, in parallel with PR-15..21, is recommended rather than deferring it to after 7 other PRs land. |
| 23 | None — fully independent of PR-15..22 | Can run at any point in parallel with the class-model track; closes D-153's revised conformance target (design doc §1). |

### Rough PR breakdown

| PR | Content |
|---|---|
| 15 | **Class model foundation (thin vertical slice).** New `Ty` class-instance variant (heap-object-pointer representation, following the `list`/`dict`/`set` pointer-to-heap-runtime-object codegen pattern in `crates/pycc_codegen/src/lib.rs`, not the by-value `tuple` struct pattern) and a new HIR class-definition representation (`HirItem` currently has only `Function`/`TopLevelStmt` — no class variant, no `Stmt::ClassDef` arm in `lower_stmt`). Scope: a single class (no inheritance), instance attributes set in `__init__`, plain instance methods with `self`, attribute get/set, method calls, instantiation. Also defines class-body statement execution order (design doc Context: eager/sequential like module top-level, reusing the *pattern* PR #358 established for module-level `def` binding but not its literal global-slot mechanism, which does not generalize to a per-class namespace). Adds `Self` (PEP 673), the self-referential deferred-annotations case (649/749), and a scoped, scalar-only single-type-parameter slice of generic classes (695, reusing PR-13's `Ty::Param` substitution mechanism). New ADR needed for the class instance layout, mirroring D-089's role for the recursive `Ty`. Does not implement `E0101`/`E0102`/`E0103`/`E0105` (explicitly scoped to a later "planned v0.7" milestone, not v0.3's shipped-feature diagnostics). |
| 16 | **Inheritance, C3 linearization, `super()`, `isinstance`/`issubclass`, `classmethod`/`staticmethod`.** Single and multiple inheritance, C3 MRO computed at compile time, zero-arg `super()` (3135), method override resolution, `isinstance`/`issubclass` against the class hierarchy (3119), `__init_subclass__`/`__set_name__` (487), `classmethod`/`staticmethod`, `__class_getitem__` (560), `@override` (698, diagnostic `T0031`). Poses an open design question: does v0.3 support runtime-polymorphic calls through a base-typed reference holding a subtype instance (e.g. a `list[Animal]` mixing `Dog`/`Cat` instances)? `TYPE_SYSTEM.md`'s "static dispatch" language for dunder protocol methods does not settle this either way — this PR's own design work decides it, and only invokes D-006 if the answer needs vtable dispatch. |
| 17 | **`@property`.** Getter/setter method transformation, attribute-access syntax redirects to method call, setter validation. |
| 18 | **Dataclasses (557) and `dataclass_transform` (681).** Auto-generated `__init__`/`__eq__`/`__repr__` from class-level annotated fields; `field()` defaults. Brings class decorators (3129) along as their first real vehicle — general function-decorator syntax stays unsupported until a PR actually needs it. |
| 19 | **Enums (435).** `class Color(Enum): RED = 1` syntax, member iteration, name/value lookup. The inherit-from-`Enum` idiom may be special-cased rather than requiring the full C3 algorithm — confirm this scoping choice empirically during the PR, don't assume it. |
| 20 | **Protocols and structural typing (544), plus remaining PEP 3119 ABC surface not covered by PR-16.** Structural (duck-typed) interface checking extension to the `Ty` system — does not require nominal C3 inheritance for the structural-conformance check itself, only the class-instance `Ty` representation from PR-15. Resolves or supersedes D-006 before implementation (design doc §3) — this PR is where "vtable dispatch only for explicit dynamic-Protocol use" actually gets implemented, once PR-16's polymorphism question has answered whether ordinary dispatch needs it too. |
| 21 | **`match` statement with exhaustiveness checking (634–636), diagnostic `T0030`.** All pattern kinds: literal, capture, wildcard, sequence, mapping, or-patterns, and class patterns (`case Point(x=0, y=0):`). Non-class pattern kinds do not depend on the class model at all — `pycc_ast` already parses `match` syntactically but doesn't re-export `StmtMatch`, and `lower_stmt` has no arm for it yet, so this is greenfield regardless of class-model status. Class patterns specifically need PR-15's class/attribute representation. `T0030` is pre-reserved, becomes real once this PR lands. |
| 22 | **Exceptions: `try`/`except`/`finally`, exception chains (`raise ... from`, 409), `except*`/`ExceptionGroup` (654), custom exception classes, `finally` semantics (no `return`/`break`/`continue` in `finally`, 765), `except A, B:` without parens (758), `OSError` hierarchy (3151).** The first implementation task must resolve or supersede D-005 (design doc §2) — the actual design work (Itanium ABI vs. SEH per Tier-1 target, verify implementability across all 5 Tier-1 targets, cost/complexity analysis of the "zero-cost happy path" claim) before writing exception-handling code, not silently assumed ready. The highest single technical-risk item in v0.3. |
| 23 | **Breadth PEP sweep closing D-153's revised conformance target.** Positional-only params (570), `Final` (591), `Annotated` (593) — the 3 of 11 originally-candidate PEPs D-153's per-PEP feasibility pass found genuinely reachable without a new prerequisite subsystem (design doc §1 has the full itemization, including why the other 8 candidates were deliberately not attempted). Fully independent of PR-15..22; can run at any point in parallel with the class-model track. |

Each PR above adds its own conformance fixture(s) to the existing `tests/conformance.rs` harness as its feature lands, so the ≥37-row D-153 target is verified incrementally, mirroring v0.2's own discipline for its ≥15-row target.

Independently of the PR-15..23 sequence above, [#779](https://github.com/rotnov/pycc/issues/779) (the test/production scratch-directory disk-fill fix) is tracked as five dependency-ordered sub-issues, Part 1 first: [#781](https://github.com/rotnov/pycc/issues/781) (shared `pycc_scratch::ScratchDir` abstraction + repo lint gate, foundational), [#782](https://github.com/rotnov/pycc/issues/782) (migrate existing test files), [#783](https://github.com/rotnov/pycc/issues/783) (fix `src/main.rs` production leaks), [#784](https://github.com/rotnov/pycc/issues/784) (bounded stale-root cleanup), and [#785](https://github.com/rotnov/pycc/issues/785) (operational `TMPDIR` guidance + verification). All five sub-issues are delivered and #779 is closed by the pull request delivering Part 5: Parts 1–4 landed the shared abstraction, the test and production migrations, and the bounded stale-root sweep, and Part 5 added the operational `TMPDIR` guidance plus the executed three-consecutive-runs verification recorded in `docs/TESTING.md`'s "Scratch directories" section.
