# 2026-08-22-09 — issue #719, PEP 701 conformance fixture (PR-1 of two)

## Overall status

Autopilot on milestone **v0.3**, running `next-milestone` → `issue-select` →
`issue-implement`. The v0.3 Accept criterion (≥ 37 `docs/PYTHON_STANDARDS.md`
matrix rows at `◐` or better) stands at **32 of 37** at `02b51f8e`, verified by
`python3 scripts/check_conformance_breadth.py` (rc=0, "32 evidence-backed rows,
all declared (2 accepted as whole-PEP, 30 subset)"). The milestone is **not met**.

## What this pull request is

The first of two, for #719. It adds `tests/fixtures/pep_0701_fstring_grammar.py`
and registers it in `tests/conformance.rs`. **The matrix row stays `☐`.**

The split is required by
[D-102](../decisions/D-102-extend-tests-conformance-rs-for-pr-9-s-9-new-pep.md),
which states the interim policy as: a row flips only once its fixture is observed
green on a real, already-completed CI run across all five Tier-1 targets in both
profiles — "recorded by hand, after the fact, never speculatively" — and lists
speculative hand-flipping among its rejected alternatives. There is also a
mechanical reason: every flipped cell cites the run that ratified it
(`docs/PYTHON_STANDARDS.md:218` and `:167`), and a flip authored in its own commit
has no run ID to cite. Repository precedent is unanimous: PEP 560 (`636a452e` then
`dcf0c2a9`), PEP 3135 (`2dcc00aa` then `b72aa980`).

Note for whoever picks this up: **no checker enforces the split.** A single-PR flip
would go green. The obligation is visible only to review.

## In flight

- **PR-1 (this one)** — fixture plus registration. Once merged, record its
  completed `main` push run ID; PR-2 cannot be written without it.
- **PR-2 (not yet opened)** — flips row 278 `☐` → `◐`, flattens the fixture path
  from `py312/pep_0701_fstring_grammar.py` (the directory does not exist, and the
  stale path becomes a hard guard failure the moment the row becomes an evidence
  row), adds the `tests/fixtures/conformance-breadth-manifest.json` entry with at
  least one `core` gap per D-177, and updates the `docs/ROADMAP.md` conformance
  counts 32 → 33 and "5-row gap" → "4-row gap". `◐`, not `✅`: conversion flags
  and format specs remain unimplemented.
  **`docs/ROADMAP.md:181` must not be touched** — both the 37 and the 39 there are
  D-153 acceptance targets, not progress counts.

## Known follow-ups

- **#729** — new. D-185 tracking issue for `tests/conformance.rs` (1276 lines).
  Filed rather than acted on because the obvious extraction is CI-red: three guard
  call sites in `tests/conformance_matrix_guard.rs` plus
  `scripts/check_conformance_breadth.py:457` substring-scan that file by hard-coded
  path, and 142 `tests/fixtures/` literals sit inside individual `#[test]` bodies.
  A correct decomposition must relocate the guards' harness input alongside the
  code, which changes the guard contract.
- **#720** — the `f"{n=}"` debug specifier is a silent wrong answer (compiles, exits
  0, omits the `n=` prefix). Deliberately excluded from this fixture; recorded as a
  `core` gap for PR-2's manifest entry.
- Within-scope selection starvation: `issue-select` step 5 now ranks milestone
  membership first (D-191, merged as `02b51f8e`), which fixed cross-scope
  starvation. Inside the scope, ordering is still by priority marker, and the
  v0.3 critical path #541 → #703 → #542 → #543 — carrying exactly the four rows
  that take 33 to 37 — is **unmarked**, so it sorts below roughly twenty in-scope
  P2 issues. Worth revisiting before the next selection.

## Where to resume

Read this file, then `docs/sessions/2026-08-22-08-issue-727-milestone-ordering-pr-opened.md`
for the ordering change that preceded it. The immediate next step is PR-1's CI, then
PR-2 citing its run ID.
