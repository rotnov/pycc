# 2026-08-22-03 — #702 Part 2: PR #715 green on every required check

## Overall status

Issue #702 ("Part 2 of #541: raising and catching user exception classes") is
implemented and delivered as pull request
[#715](https://github.com/rotnov/pycc/pull/715), head `821d8ed4`, branched from
`origin/main` at `60315e3b`. It is **not merged** — the dispatching session
merges.

`closingIssuesReferences` verified at head `821d8ed4`: `totalCount: 1`, naming
only #702. #541 stays open for Part 3 (#703).

## Check state at head `821d8ed4`

Both required contexts (`ci-gate`, `audit`) pass, along with `governance`,
`build-test-coverage`, `frontend-perf-gate`, `cross-compile-build`,
`cross-compile-verify`, `status-page-freshness`, and all four
`native-build-test` matrix entries.

The `Pages` workflow's `build` job fails. It is **not** a required context
(`main`'s required set is `["ci-gate","audit"]`), the failure reproduces
identically at the untouched base, and it was introduced on `main` by #710
(`a24b721d`), which changed `site/status/index.html` without bumping
`site/sitemap.xml`'s `<lastmod>` for `/status/`. Filed as
[#716](https://github.com/rotnov/pycc/issues/716) (P1, milestone v0.3); the
first failing `main` run is
[32539130933](https://github.com/rotnov/pycc/actions/runs/32539130933).

## Follow-ups opened

- [#704](https://github.com/rotnov/pycc/issues/704) — retargeted, not
  duplicated: partial shadowing of a builtin exception name panics in
  `pycc_types` instead of emitting a diagnostic (P2, v0.3).
- [#714](https://github.com/rotnov/pycc/issues/714) — binding a user exception
  subclass as a value compiles, then aborts at runtime with
  `NameError: 'Exception.__init__'` (P2, v0.3). Confirmed distinct from #711.
- [#716](https://github.com/rotnov/pycc/issues/716) — the `Pages` build
  breakage above (P1, v0.3).

## Notes for a fresh session

- The R1 invariant holds: the `Ty::Instance` predicate in
  `crates/pycc_types/src/exception.rs` is byte-identical to base. Acceptance is
  keyed structurally on `HirExpr::Call`; `raise <bound value>` is still `T0021`.
- The frontend perf claim is backed by an absolute measurement at the base
  commit in a separate worktree (7.9938 µs) against HEAD (7.6910 µs).
  Criterion's own saved baseline postdated the first work commit, so its
  "No change in performance detected" verdict was self-referential.
- Two local test-failure classes are environmental and reproduce at the
  untouched base: ~48 failures reading `conformance oracle must be exactly
  Python 3.14.7, found "Python 3.14.6"`, and two in
  `scripts/test_check_pages_performance_budget.rb` about unexpected images.
