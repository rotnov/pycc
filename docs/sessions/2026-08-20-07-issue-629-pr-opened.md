# 2026-08-20-07 — #629 implemented, PR #640 opened, autopilot paused

## Overall status

The `/next-milestone` autopilot run is on milestone **v0.3**, which is not
met: D-153 requires at least 37 conformance rows at `◐` or better and the
tree currently carries 31. This checkpoint covers one full
`issue-select` → `issue-implement` iteration on issue
[#629](https://github.com/rotnov/pycc/issues/629) ("Part 1 of #20: Resolve
the pycc_rt artifact by profile, target triple, and CARGO_TARGET_DIR").

Baseline for this iteration: `origin/main` at `5689ad19`, re-verified
immediately before this entry was committed. Zero open pull requests at
baseline; `tests/fixtures/policy-successor-manifest.json` steady-state, so no
run-wide D-103 block.

## In flight

**[PR #640](https://github.com/rotnov/pycc/pull/640)** — open, three commits
on branch `issue-629-cargo-target-dir`, `Fixes #629`. **Not merged.** CI was
still running when this entry was written; the merge decision is the next
session's, after a full diff re-read per `issue-implement` step 8.

What it delivers:

- A new `crates/pycc_codegen/src/artifact_layout.rs` module owning artifact
  path resolution, moved out of `src/main.rs`. The resolver honors
  `CARGO_TARGET_DIR`, then `CARGO_BUILD_TARGET_DIR`, then a manifest-relative
  `target/`. `find_pycc_rt_lib_dir_in` takes an already-resolved target root,
  so the `.join("target")` that caused the bug has no remaining home.
- `tests/target_dir_literals.rs`, a mechanical gate forbidding the literal
  `target/` and `.join("target")` in non-comment Rust under an explicit
  source allowlist. It is a `#[test]`, deliberately not a CI step, so that no
  manifest-protected workflow file is touched and the change stays one PR.
- `docs/decisions/D-183-honor-cargo-target-dir-when-locating-build.md`, which
  narrows one derivation sentence of D-091 item 4.
- The harden batch pass over the review findings, and the corresponding
  `docs/AGENT_RETROSPECTIVE.md` entry.

**The second precedence level was not in the filed issue.** A review round
challenged the prose claim that Cargo exports nothing for the
`build.target-dir` config key; measuring it showed `CARGO_BUILD_TARGET_DIR`
*is* honored, so the resolver as first written still failed for a config-file
redirect performed through Cargo. That widening exists because a review
finding was measured rather than accepted or dismissed.

Local gates, all captured un-piped: `fmt --check` 0 · `clippy --workspace
--all-targets -D warnings` 0 · `cargo test --workspace` 0 (49 suites) ·
`cargo llvm-cov --workspace --fail-under-lines 100 --fail-under-regions 100`
rc=0, TOTAL 100.00%/100.00%, 0 missed · `cargo doc` 0 ·
`generate_decisions_index.py --check` 0 · `check_roadmap_evidence.rb` 0 ·
`test_check_roadmap_evidence.rb` 0 · `check_ci_permissions.rb` 0 ·
`test_check_ci_permissions.rb` 0 · `unittest discover -s scripts` 0 ·
`validate_agent_policies.py` 0.

Review loop: five rounds with the pinned `ievo:deep-reviewer` (binding
verified through `scripts/check_claude_reviewer_binding.py`, rc=0,
`ievo@ievo-skills 0.78.8 OK`), closing clean on round 5. Four of the five
rounds found a member of one class — an unmeasured claim about Cargo's
behavior in prose adjacent to the change. All were fixed on the branch; none
reached `main`.

## Known follow-ups

- **[#639](https://github.com/rotnov/pycc/issues/639)** (v0.3, filed this
  session) — decide whether Cargo's `--target-dir` flag and a config-file
  `build.target-dir` should be honored at all. D-183 asked for this to be
  filed; the three passages that said "untracked by any issue yet" now cite
  it.
- **#20** stays open. #629 is Part 1; **#630** (real build dependency) and
  **#631** (CI ordering, touches D-103-protected `ci.yml`) both depend on it
  and are unblocked once #640 merges.
- `.harden/incidents/documentation-sweep-stops-at-the-changed-file/` is
  recorded `verdict: pending` — an open entry. Its identified promotion is a
  lexical reference-integrity gate, sibling to the one #640 ships,
  deliberately not built inside the PR whose review surfaced the class.
- `.harden/incidents/unmeasured-claim-about-external-tool-behavior/` is
  recorded `build nothing` with its reasoning; the artefact proposal is in
  #640's body, which is the approval surface for an unattended run.

## Honest gaps in this iteration

- **`issue-select` step 7's adversarial advisor round on the #629 selection
  was not run.** The selection was carried into `/issue-implement` directly.
  Nothing downstream refuted the pick, but the round is a required step and
  it was skipped, not passed.
- **The harden batch clustering was performed inline**, in the orchestrating
  session, rather than through the dispatched tracer `references/batch.md`
  specifies. Deliberate — the session held all 13 findings and the full
  five-round history — and recorded in both incident entries.
- **`issue-select` step 2's milestone triage over the unassigned open issues
  was not completed** this iteration. Clusters #544–#552 and #563–#569 were
  previously left unassigned on purpose as cross-cutting; the rest of the
  unassigned pool has not been walked.

## Paused autopilot

- **Directive scope:** `/next-milestone` with no arguments — loop over
  milestones, not one. Baseline the default-branch tip, pick the first
  `## vX.Y` section whose Accept bullet is unmet with cited verified
  evidence, adopt it without confirmation (D-127), hand off to
  `issue-select`.
- **Active milestone:** v0.3 — **not met** (31 conformance rows at `◐` or
  better against D-153's required 37).
- **Last iteration outcome:** #629 implemented; PR #640 opened and awaiting
  CI. Not merged.
- **Exact next autopilot step:** `issue-implement` step 7/8 for PR #640 —
  watch CI through `.claude/skills/gha-watch-ci-pr/scripts/ci-watch.sh
  rotnov/pycc 640`, re-read the full diff, merge with a merge commit, delete
  the branch, confirm #629 closed. Then re-enter `issue-select` step 1 with a
  fresh baseline.
- **In-run denylist:** empty. No issue reached a per-issue stop condition
  this run.

## Where to resume

- `docs/ROADMAP.md`'s v0.3 section for the milestone Accept bullet.
- `.claude/skills/issue-select/SKILL.md` `## Loop` for the re-entry contract.
- `.harden/findings/issue-629.jsonl` and the two incident directories named
  above for the hardening state.
