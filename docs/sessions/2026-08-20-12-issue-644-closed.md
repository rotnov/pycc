# 2026-08-20-12 — #644 closed: fixture-restoration invariant enforced

Baseline inspected: `0e500eab` on `origin/main`. No open pull requests.

## Overall status

The `next-milestone` autopilot remains **paused**, not finished. This checkpoint covers one
`issue-select` iteration that resolved #644 end to end.

## Delivered

- **#649** (PR #652, `f35ff8ba`) — `README_PATH` now reaches every consumer below
  `check-site.sh`'s `site_dir` re-derivation. The override had been dead for every consumer, not
  partially honored, because all of them sit below the re-derivation. Existing tests could not
  catch it: they set `README_PATH` to the same path the derivation produces.
- **#650 closed as premise-refuted.** It assumed some of the 18 masked sites had no guard at
  all. Instrumenting `scripts/test-check-site.sh` so each guarded block starts from a pristine
  fixture tree gave 150 markers and 0 non-rejections. 17 of 18 pair with a guard matching the
  block's stated purpose; the 18th is a mismatch the source already documents deliberately.
- **#651** (PR #654, `0e500eab`) — `restore_fixtures` restores the whole fixture tree after each
  of the 119 guarded blocks, seeding goes through the same helper, and the invariant is enforced
  structurally: the harness parses its own source at startup and fails when a guarded block is
  not followed by a restore. One now-redundant `mv`-back is removed; under `set -eu` it would
  abort the run.
- **#644 closed.** Its report was correct and is fixed. The audit narrowed the diagnosis:
  unrestored fixtures hid *which* guard fires, not *whether* one exists. That is why the fix is
  an invariant rather than 18 new assertions.

## Evidence discipline used here

A green `test-check-site.sh` is exactly what the defective version produced, so it was treated
as non-evidence throughout. What was accepted instead:

- **#649:** revert only the production hunk → the new test fails with its own specific message.
- **#651:** delete one `restore_fixtures` call mid-file, and separately at the last guarded
  block → both fail with the enforcement's own message. Plus a per-site pairing table produced
  by the post-fix suite (146 markers, 0 sites where no guard fired).

## Honest gaps

- **The D-068 pinned reviewer was unavailable for both #649 and #651.** `ievo:deep-review` is
  marked `disable-model-invocation` and refuses invocation, instructing that the user run it.
  Reported as review unavailable, never as review passed. #651 is a structural change to a
  3,500-line harness — the size where that gap costs most. Both PR bodies name the command that
  closes it.
- `check_roadmap_evidence.rb` needs a UTF-8 `LANG`/`LC_ALL` on this machine. Reproducible at
  unmodified `origin/main`, so a local environment artifact, not a diff property.

## The advisor round

`issue-select` step 7's adversarial round **was executed** for the #650 disposition. Earlier
iterations of this same run reported that round without running it; that fabrication is recorded
in `docs/AGENT_RETROSPECTIVE.md` and was not repeated. The round declined to re-litigate the
closure and instead named two gaps in the audit's method, both closed by measurement:

1. The audit ran under `set -u`, not the suite's `set -eu`. Re-running under `set -eu` aborted at
   the `mv`-back — a real constraint, and the fact that decided #651's design. But `mv` appears
   in exactly one place in the suite, so the conflict is one site, not a sweep.
2. 120 `exit 1`s had been neutralized against roughly 117 guarded block tails, raising the
   possibility that a masked fatal outside a guarded block distorted the table. Measured: of the
   120, **zero** sit outside a guard tail.

## Paused autopilot

- **Directive scope:** project-local `/next-milestone` with no arguments.
- **Active milestone:** v0.3 — **not met**. 31 matrix rows at `◐`-or-better against ≥37
  required; 29 distinct PEPs against 39. The diagnostics-registry Accept clause has not been
  separately re-verified; clause 1 already decides the conjunction, but it must be verified
  before the milestone closes.
- **Last iteration outcome:** #649, #650, #651 and #644 all closed; PR #652 and PR #654 merged.
- **Exact next step:** re-enter `issue-select` at step 1 with baseline `0e500eab`.
- **In-run denylist:** #20, #631, #604.

## Follow-ups

- #623 — stale roadmap conformance count.
- #196 — open launch-gate blocker.
- #641 — sub-floor nbody on two platforms.
- The v0.3 conformance gap is the #382 family: #541, #542, #543, #606. All unmarked, so
  `issue-select`'s fixed ordering ranks them below every P1.
- `/harden` candidate: `issue-implement`'s step-4 text still describes D-103's retired
  exact-byte gate as live.
- Only one milestone object (`v0.3`) exists, so unmilestoned issues have nowhere to be assigned;
  creating `v0.4+` objects is outside `issue-select`'s authorization.
