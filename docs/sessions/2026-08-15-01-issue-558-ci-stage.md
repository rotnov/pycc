# Issue #558 CI routing stage checkpoint

## State

- Refreshed base: `origin/main` at `30ff72bb2f9d89fc4824a90bdc3e0e7f5cbf356a`.
- Stage payload head before this checkpoint: `6526758a372d40ece9c9018350e434e5574e24c7` on `codex/optimize-ci-feedback`, 12 commits ahead and 0 behind the base.
- Issue #558 is open, assigned to `rotnov`, with `enhancement` and `in progress` labels.
- The only open pull request is #518, open and mergeable at head `7bfc8984c5c1dc76cb7c7d81ab5058ebf8e07e5f`. Its `.agents/`, `.claude/`, and `.harden/` changes do not overlap this stage's files or protected-successor sequence.
- The live `.github/workflows/ci.yml`, `scripts/check_roadmap_evidence.rb`, and `scripts/test_check_roadmap_evidence.rb` remain byte-identical to `origin/main`.

## Staged transition

- `scripts/classify_ci_changes.py` and its test are steady-state protected targets.
- The checker and checker-test targets point to `check_roadmap_evidence-d171.rb` (`70b2b286...`) and `test_check_roadmap_evidence-d171.rb` (`cf9a6578...`).
- The final CI successor is protected at `ci-d171.yml` (`785e6415...`), while the live CI target remains self-sourced at its existing bytes.
- D-171, its design/plan, manifest transition, and staged-current-state testing documentation are included. No speedup is claimed before the required 20-PR post-activation sample.

## Local verification

Passed:

- `cargo doc --workspace --no-deps`; x86_64 `pycc_rt` build; `cargo build --workspace`; release `pycc_rt` build; and the macOS arm64-to-x86_64 cross-build (Mach-O x86_64 output).
- `cargo test --workspace -- --skip init_reports_an_unavailable_cwd_without_panicking` after the required workspace/release preparation, with zero failures; `cargo clippy --workspace --all-targets -- -D warnings`.
- Focused classifier tests (19), full Python discovery (851, 6 skipped), agent policy/assets validators, and both Codex and Claude offline alpha evals.
- Live CI-permission tests/checker (42 tests, 155 assertions), live roadmap tests/checker (186 tests, 673 assertions), staged D-171 roadmap tests/checker (213 tests, 1,054 assertions), README coverage binding (24 tests, 28 assertions), and replicated performance-checker tests (17 tests, 24 assertions).
- Frontend throughput at 38.57 ms against the 75 ms ceiling; both CI-monitoring shell tests; both marketplace validators; `actionlint`; and safe YAML parsing for every live workflow plus `ci-d171.yml`.

Local/CI boundary:

- The exact deleted-current-directory test `slice0::init_reports_an_unavailable_cwd_without_panicking` timed out in two bounded focused probes (20 seconds and 5 seconds). The harness was waiting on `pycc init`; a direct probe reached Darwin's uninterruptible `U` state. The test, `src/main.rs`, Cargo inputs, and toolchain bytes are identical to `origin/main`, and the branch has no Rust/build-input diff, so this is a base-identical macOS 26.5.1 local behavior, not evidence of a Stage 1 regression. CI remains authoritative for the unmodified full workspace command.
- Local `python3.14` is 3.14.6, not the required 3.14.7. The focused ignored nbody gate rejected that version exactly; the 36 ignored conformance tests and exact ignored nbody execution are CI-only.
- The exact coverage command requires the reviewed passwordless `sudo -u nobody` boundary; `sudo -n` requires a password on this host. No weaker local coverage result is substituted. Tier-1 runners on other OS/architectures, Intel-native cross execution, GitHub artifact-bound paired performance, and both Lighthouse jobs are also CI-only.

## Next activation step

Publish this as Stage 1/3 without `Fixes #558`, require `audit` and `ci-gate`, and merge with a merge commit only after live-state and review-thread verification. Then start Stage 2 from the exact post-merge `origin/main`: copy only the staged checker and self-test byte-for-byte to their live paths, return those manifest entries to self-source, and redirect the still-unmodified CI target to the protected `ci-d171.yml` successor. Do not activate CI until that checker-activation pull request has merged.
