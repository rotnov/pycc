# 2026-09-03-01 -- Issue #749: the PEP 681 row is recorded as structurally blocked

## Status: delivered

Worktree `.claude/worktrees/autopilot-2026-09-02-08`, branch
`fix/issue-749-pep681-row`, started from `origin/main` at `38adaac2`.
[#749](https://github.com/rotnov/pycc/issues/749) (v0.4) was delivered by
[PR #900](https://github.com/rotnov/pycc/pull/900), squash-merged as
`6821643b`; the issue is closed. The governing decision record is the
pre-existing
[D-196](../decisions/D-196-pep-681-is-a-second-itemized-row-permanently.md) --
this change adds no new ADR. Plan:
<https://github.com/rotnov/pycc/issues/749#issuecomment-5522088143>.

## How this task was run

One autopilot iteration under the standing `fix all opened issues`
directive with milestone scope v0.4: D-021 preflight, `issue-select`, then
`issue-implement` with `issue-to-plan` and the implementation each
dispatched into isolated agents (D-142/D-143), a D-068 `ievo:deep-reviewer`
round plus its fix round, the `/harden batch` pass, and this file.

Two orchestrating agents died mid-iteration on API rate limits (the first
during preflight, the second after PR #900 reached a green CI verdict). The
dispatching session held an independent `ci-watch.sh` watch on #900 under
`Monitor`, and finished the iteration itself: re-read the full diff,
confirmed `mergeStateStatus: CLEAN`, `closingIssuesReferences.totalCount: 1`
(#749) and zero unresolved review threads, then merged and wrote this
snapshot. The selection-round detail below is therefore what the
implementation and the tree record, not a report the selecting agent
survived to write.

## What changed

- `tests/fixtures/pep_0681_dc_transform.py` deleted -- it was never
  registered in the conformance harness and cannot be, since pycc treats
  `@dataclass_transform()` as `@dataclass` while CPython's
  `typing.dataclass_transform` synthesizes nothing at runtime, so any
  fixture exercising the synthesized methods diverges from the oracle by
  construction.
- `tests/conformance_matrix_guard.rs`: the PEP 681
  `UNREGISTERED_FIXTURE_ALLOWLIST` entry is gone; the constant now holds one
  tuple (the documented `pep_0526_var_annotations_smoke.py` throwaway).
- `docs/PYTHON_STANDARDS.md:353`: the PEP 681 row's Test cell is now
  "no fixture -- structurally blocked ... (D-196, #749)", status `☐`.
- `docs/ROADMAP.md`, `docs/superpowers/specs/2026-08-06-v0-3-classes-pattern-matching-design.md`:
  the sentences that listed the deleted fixture, or attributed the
  divergence to the closed and unrelated #248, now cite D-196/#749.
- Comment-only retargets of the same stale `#248` pointer in
  `crates/pycc_types/src/tests.rs:23054`, `tests/conformance/classes.rs:297`
  and `tests/fixtures/pep_3129_class_deco.py:21` (the fixture's
  non-comment lines are byte-identical, so its oracle stdout is unchanged).

## D-068 review and harden batch

Review round 1 found the two stale `#248` comments under `tests/` that the
plan's directory-bounded (`crates/`) search had missed; the fix round
(`39f02a03`) ran the tree-wide search, adjudicated every remaining `#248`
hit and recorded the search plus its hit count, and the following round was
clean. The Codex bot thread on #900 was answered and resolved.

The `/harden batch` pass filed this as recurrence 6 (seventh file) of
`documentation-sweep-stops-at-the-changed-file`, with verdict `zero` and
**nothing built**: the class is at the review rung, which caught every
member and bounded the cost to one round, and the topic already measured a
textual rung at zero in the #729 iteration. The variant is one phase
earlier than the previous entries -- the extent was fixed by the *plan's*
search scope, not by the reviewer's cited sites -- and the next rung if it
recurs is named in the incident file: a diff-scoped retarget check over
dropped `#NNN`/`D-NNN` references. No `docs/AGENT_RETROSPECTIVE.md` entry
was added; the lesson ("cited sites are examples of the class, not its
boundary") is already recorded there and promoted into `issue-implement`
step 5.

## Tracker state at merge

111 open issues, 69 of them with no milestone -- still far above D-192's
ceiling of 20, so no new non-milestone issue may be filed except in place
of one that closes. The 4:1 non-milestone merge quota is unspent: the four
preceding issue-closing merges (#900 -> #749, #897 -> #890, #880 -> #729,
#878 -> #868) are all v0.4.

## Known follow-ups

- #879 (the location-claim static checker) remains filed, unassigned, no
  milestone.
- A concurrent peer session filed #881--#895 as v0.4 issues on 2026-09-02
  and has been delivering them (#890 via PR #897). Check open PRs, remote
  branches, and issue comments before selecting from that range.

## Where to resume

`docs/sessions/` newest-first, then `git log origin/main` for anything
after `6821643b`.

## Paused autopilot

- Directive scope: open-ended (`fix all opened issues`); milestone scope
  v0.4 in effect.
- Last iteration outcome: #749 delivered (#900 -> `6821643b`).
- Next step: fresh D-021 baseline from `origin/main`, then re-enter
  `issue-select` (v0.4 membership first).
- In-run denylist: none.
- Known follow-ups not filed: none.
