# 2026-09-05 — #937: flip the PEP 563 row to `◐` after the observed-green `main` run

## Previous checkpoint's outcome

The iteration before this one delivered
[#919](https://github.com/rotnov/pycc/issues/919) (`from __future__` imports
as compile-time no-ops, D-229): PR [#938](https://github.com/rotnov/pycc/pull/938)
merged by squash as `51808cd8` at 2026-09-05T14:45:02Z, issue #919 is CLOSED,
and its branch was deleted. Every post-merge `main` run for `51808cd8` is
green: CI
[33972731538](https://github.com/rotnov/pycc/actions/runs/33972731538), Pages
[33972731542](https://github.com/rotnov/pycc/actions/runs/33972731542), Main
history audit
[33972731585](https://github.com/rotnov/pycc/actions/runs/33972731585), and
Status page freshness
[33972731551](https://github.com/rotnov/pycc/actions/runs/33972731551).
`autopilot/iter-2026-09-05-09` was cut from that same commit, which was still
`origin/main` when this snapshot was written.

## Overall status

Implemented [#937](https://github.com/rotnov/pycc/issues/937) on
`autopilot/iter-2026-09-05-09`: a documentation-and-manifest change with no
Rust diff, so no coverage run applies. One pull request, body carrying
`Fixes #937`; the orchestrating session watches CI and merges.

## What the change is

The D-102 / rule 5 observation the flip depends on: CI run 33972731538 (the
`main` push for `51808cd8b1261d87900601679e33a8a5416ec6dc`, PR #938's merge)
completed SUCCESS with
`pep_0563_lazy_annotations_matches_cpython_3_14_7_byte_for_byte ... ok` on all
five Tier-1 jobs (`build-test-coverage` on macos-14 plus the four
`native-build-test` matrix legs); the test runs the debug and release
profiles internally. `gh run view 33972731538 --log | grep -c "pep_0563.*ok"`
reported exactly 5.

- `docs/PYTHON_STANDARDS.md`: the PEP 563 row is `◐`; policy note 13 now
  cites the run in the same shape note 12 uses and names the three `core`
  gaps that keep the row off `✅`.
- `tests/fixtures/conformance-breadth-manifest.json`: a new row for PEP 563
  (three `proven` categories drawn from the fixture; `core` gaps: a forward
  reference to a later-defined name, a string annotation, and
  `__annotations__` introspection). Extending note 13 shifted the matrix by
  four lines, so every existing `matrix_line` is bumped by 4 (the #919
  precedent, which bumped by 11).
- `docs/ROADMAP.md`: the conformance headline is re-derived to 39 rows /
  40 distinct PEPs (still past v0.3's 37 / 39 floor) with the checker's
  summary quoted verbatim, plus a short "PEP 563 flipped" paragraph.

Gates run locally: `python3 scripts/check_conformance_breadth.py`,
`RUBYOPT="-E UTF-8" ruby scripts/check_roadmap_evidence.rb`,
`ruby scripts/check_status_page_freshness.rb origin/main`,
`RUBYOPT="-E UTF-8" bash scripts/check-site.sh`, and
`python3 -m unittest discover -s scripts -p 'test_*.py'` (986 tests, OK).

## Deviations from the plan

- `ruby scripts/check_roadmap_evidence.rb` without `RUBYOPT="-E UTF-8"`
  fails locally with `invalid byte sequence in US-ASCII` on an unchanged
  `origin/main` copy of the roadmap as well, so it is a shell-locale artefact
  rather than this diff; the UTF-8 invocation passes.
- `check_status_page_freshness.rb` did not fire on the headline change, so
  `site/status/index.html` and its four pins are untouched.
- `docs/PYTHON_STANDARDS.md` grew from 38,569 to 38,851 bytes against its
  43,008-byte llms.txt budget; `check-site.sh` passes.

## Known follow-ups

- [#889](https://github.com/rotnov/pycc/issues/889) — string-literal and
  attribute-qualified annotations (`C0001` today); closing it also closes the
  row's second `core` gap.
- [#882](https://github.com/rotnov/pycc/issues/882) — the remaining
  `pycc_std` typing-surface widening; its `__future__.annotations` candidate
  was superseded by #919.
- A future import inside a function/block body keeps the generic block-import
  `C0001`; one inside `if TYPE_CHECKING:` is silently accepted. Both recorded
  in D-229, neither locked in by a test.
- The later-defined forward reference and `__annotations__` introspection
  gaps have no tracking issue yet; they are recorded only in the manifest.

## Paused autopilot

- Directive scope: open-ended (`/goal fix all opened issues`).
- Active milestone: `v0.4` (Accept unmet).
- Last iteration outcome: #938 merged (`51808cd8`), #919 closed.
- This iteration: #937 implemented; PR open, awaiting CI and merge by the
  orchestrating session.
- Next step: re-enter `issue-select` for `v0.4` after this PR lands.
- Denylist: empty.

## Where to resume

`docs/PYTHON_STANDARDS.md` note 13 and the PEP 563 manifest row are the
whole change; `scripts/check_conformance_breadth.py` is the gate that ties
the row marker, the manifest, and the ROADMAP headline together. If the
matrix shifts again, bump every `matrix_line` and re-run the checker until
green.
