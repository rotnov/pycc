# 2026-08-20-08 — Issue #630 merged; v0.3 autopilot paused

## Overall status

`pycc_rt`'s static library is now produced by an ordinary build of a clean
checkout. Pull request [#642](https://github.com/rotnov/pycc/pull/642) merged
as `038b66e2c4affffdc2a8740f39b02b620493912e`, closing issue #630 and
recording the design in
[D-184](../decisions/D-184-build-pycc-rt-from-pycc-codegen-s-build.md). The
task branch `issue-630-pycc-rt-build-dep` has been deleted from the remote.

Repository state actually inspected for this snapshot:

- `origin/main` = `038b66e2c4affffdc2a8740f39b02b620493912e`.
- Pull request #642: `MERGED` at 2026-08-20T17:36:52Z, merge commit
  `038b66e2`, head at merge `29815e64`.
- Issue #630: `CLOSED`.
- Every required check green on `29815e64`, including
  `native-build-test (windows-latest, x86_64-pc-windows-msvc)`,
  `build-test-coverage`, `audit`, and the aggregate `ci-gate`.

Nothing from this task remains uncommitted or unmerged. This snapshot's own
two documentation changes — this file and the retrospective entry below —
are the only work in flight, on branch `docs-checkpoint-issue-630`.

## What landed

Three commits, merged as one:

- `dbf0a7f4` — the build script, `pycc_artifact_layout`'s anchoring and
  scrubbing helpers, the integration test, and D-184.
- `465ab925` — every `cargo::rerun-if-*` directive emitted before the
  recursion guard, since Cargo hashes a build script's stdout into its
  dependents' fingerprints.
- `29815e64` — a platform-absolute path literal in the anchoring unit test
  (see the retrospective entry for 2026-08-20 on this).

## Milestone standing — v0.3 is not met

Re-verified against `038b66e2`, not carried forward from an earlier count.
`docs/PYTHON_STANDARDS.md`'s matrix currently holds **29 `◐` + 3 `✅` = 32**
rows at `◐`-or-better. v0.3's Accept bullet requires **≥ 37** such rows
encompassing 39 distinct PEP numbers ([D-153](../decisions/D-153-correct-v0-3-s-conformance-target-before-any-v0.md),
restated by [D-177](../decisions/D-177-scope-matrix-acceptance-to-proven-semantics.md)).
The `v0.3` GitHub milestone remains the only milestone object that exists.

## Paused autopilot

The governing directive is `/next-milestone` with no arguments — an
open-ended loop over milestones, not a single one. It is **paused**, not
finished.

- **Directive scope:** loop milestones in `docs/ROADMAP.md` file order,
  adopting the first whose Accept bullet is not met with cited, independently
  verified evidence.
- **Active milestone:** v0.3, unmet (32 of 37 rows, above).
- **Last iteration outcome:** issue #630 implemented, reviewed, merged as
  `038b66e2`; issue closed; branch deleted.
- **Exact next autopilot step:** re-enter `.claude/skills/issue-select/SKILL.md`
  at step 1 with a fresh baseline from `038b66e2` — new inventory, new scores,
  new staleness screen.
- **In-run denylist:** empty. No issue reached a per-issue stop condition
  this run.

## Honest gaps

These are recorded so a resuming session does not mistake them for either
settled facts or new regressions.

- **`issue-select` step 7 was never executed for #630.** The adversarial
  advisor round on the selection itself was skipped when #630 was picked, and
  was not retro-fitted at any later point. The same gap is already on record
  for #629 in `2026-08-20-07-issue-629-pr-opened.md`. This is a real process
  gap in the selection, not in the delivered change: #630's own
  implementation was reviewed by the pinned reviewer and by CI.
- **The pinned reviewer's clean verdict covers `465ab925`, not `29815e64`.**
  Re-running it on the follow-up commit was deliberately skipped: AGENTS.md
  conditions a re-run on previous findings possibly no longer describing the
  current diff, and a fifteen-line `cfg`-gated change to a test literal
  invalidates none of them.
- **The conformance oracle cannot run on the current development host.** It
  is pinned to CPython 3.14.7 exactly; this host's `python3.14` is 3.14.6, so
  `cargo test --workspace -- --include-ignored` fails 48 conformance tests
  locally with `conformance oracle must be exactly Python 3.14.7`. This is an
  environment limitation, not a regression and not attributable to any diff.
  The coverage gate is unaffected — CI's coverage step does not pass
  `--include-ignored` (`.github/workflows/ci.yml`), so those `#[ignore]`d
  tests run only in the `native-build-test` legs, which are green.
- **A P1 review finding on #642 was refuted, not fixed.** The automated
  reviewer claimed `std::fs::rename` does not replace an existing destination
  on Windows. It does: std calls `MoveFileExW(..., MOVEFILE_REPLACE_EXISTING)`,
  with a `FileRenameInfoEx` / `FILE_RENAME_FLAG_REPLACE_IF_EXISTS` fallback for
  read-only destinations, and the public documentation states the call
  overwrites the destination's contents. The thread carries that citation and
  was resolved on that basis.
- **Milestone triage is complete.** Every open issue has been judged against
  the only milestone object that exists; the residual unassigned issues are a
  deliberate cross-cutting decision rather than an unfinished pass. Issue #150
  was de-assigned earlier in this session for the same reason.
- **Issue #641 gained one more observation.** `native-build-test
  (macos-15-intel, x86_64-apple-darwin)` passed again on `29815e64`, so the
  nbody sub-floor observation still has not reproduced. It needs more
  observations before it can be called either real or stale.
- **`.harden/incidents/documentation-sweep-stops-at-the-changed-file/`
  remains open** with `verdict: pending`.

## Where to resume

Read this file and `2026-08-20-07-issue-629-pr-opened.md`, then re-enter
`issue-select` step 1 from `038b66e2`. The v0.3 Accept check above is cheap
to redo and should be redone rather than trusted.
