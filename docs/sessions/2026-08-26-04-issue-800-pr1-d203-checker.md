# Session handoff: issue #800 PR-1 — D-203 checker-side authorization

## Status

PR-1 of issue #800 (milestone v0.3), implemented against `origin/main` at
`587c8af11d3b79c9df17dd38af71a4aee141519c` (the PR #780 merge; re-verified
unmoved by `git fetch --prune` at implementation start) on branch
`feat/issue-800-pr1-d203-checker-authorization`, from the reviewed
three-round implementation plan published at
<https://github.com/rotnov/pycc/issues/800#issuecomment-5427644875>.
This entry lands with PR-1's merge (D-192). Issue #800 stays open until
PR-2 (activation) merges.

## Decision-number re-resolution

Per the plan's §4 PR-1 item 2, the indicative D-203 number was re-resolved
at implementation time: `docs/decisions/` on refreshed `main` still ends
at the duplicate D-201/D-202 pairs (the pre-existing numbering defect that
issue #803 tracks — out of #800's scope, deliberately not fixed here), and
none of the open PRs (#801, #793, #791) adds a D-203 file (#793's decision
files are stale-base copies of D-085/D-200). **D-203 held** and is used on
every surface: the Ruby constants, the heredoc's shell comments, the
`test_d203_*` names, the ADR filename, and the D-172 annotation.

## What PR-1 delivers (checker-side only; zero CI behavior change)

- `scripts/check_roadmap_evidence.rb` — three purely additive edits, with
  `D91_VERIFY_REVISIONS_SCRIPT` byte-identical to `main`'s (verified two
  ways: `git diff` shows zero deleted lines in the file, and the constant
  loaded from `HEAD`'s revision byte-compares equal to the working tree's):
  - `D203_VERIFY_REVISIONS_SCRIPT`: byte-identical to the D91 heredoc
    except the final two diff lines, which become the symmetric
    `grep -vxF 'pycc_scratch = { path = "crates/pycc_scratch" }'` filter
    on both tails plus a three-line `# D-203:` shell comment (ASCII, at
    the heredoc's 2-space indent). The new heredoc was generated
    mechanically from the existing D91 bytes so the shared prefix is
    identical by construction.
  - `D203_SCRATCH_DEVDEP_FRONTEND_PERF_MEASURE_STEPS`/`_JOB`: deep-copied
    from the D112 pair with only the verify step's `run` body swapped.
  - One new `elsif` in `validate_source_aware_perf_gate_lifecycle`, after
    the D112 branch, returning the unchanged D112/D114 gate-job set.
- `scripts/test_check_roadmap_evidence.rb` — the `run_d91_verify_revisions`
  helper gains a `script:` keyword (default `D91_VERIFY_REVISIONS_SCRIPT`;
  existing call sites unchanged) plus twelve new tests: tolerated
  addition/removal of the exact line, hard-aborts with the line present
  and criterion bumped, on another added line, on deletion of
  `serde_json` (issue #800 completion criterion 2's deletion case), on
  path/spacing variants of the tolerated line, the three D-091 invariant
  mirrors under the new script, a freeze-guard tripwire
  (`grep -vxF` absent from D91's constant, present in D203's), and two
  lifecycle tests (acceptance with either reviewed gate shape; rejection
  of a D-203-shaped body filtering a different literal).
- `docs/decisions/D-203-narrow-the-d-091-bench-manifest-tail-check-to.md`
  (accepted) recording the filter design, the two-PR coexist-then-retire
  necessity (base-owned `audit` validates head ci.yml against the pre-PR
  checker's constants), all rejected alternatives, and residual risks;
  `docs/decisions/README.md` regenerated.
- `docs/decisions/D-172-...md`: one dated update line marking D-203 as
  the recorded counterexample class to its "ordinary CI changes use one
  PR" consequence.

## Gates (all green at this snapshot, macOS local run, UTF-8 locale prefix)

- `ruby scripts/test_check_roadmap_evidence.rb`: 237 runs (was 225),
  1222 assertions, 0 failures, 0 errors, ~11 s.
- `ruby scripts/check_roadmap_evidence.rb`: passed, ~0.1 s (live ci.yml
  still matches the D112 branch — coexistence confirmed).
- `python3 -m unittest discover -s scripts -p 'test_*.py'`: 955 tests,
  OK (skipped=6), ~46 s.
- `ruby scripts/check_ci_permissions.rb`: passed (10 files).
- `python3 scripts/check_scratch_dir_usage.py`: passed.
- `python3 scripts/generate_decisions_index.py docs/decisions
  docs/decisions/README.md --check`: up to date.
- D-021 preflight: `cargo doc --workspace --no-deps` green;
  `python3 scripts/manage_ci_bypass.py status` matched the documented
  baseline.
- CI expectation per the plan: this PR routes to EMPTY_SELECTION (docs/ +
  scripts/ paths only); the `governance` job runs the new tests, and
  `audit` accepts the unchanged head ci.yml via the existing D112 branch.

## Pending — NOT delivered by this PR

- **PR-2 (activation)** does everything CI-visible: the narrowed
  "Verify exact benchmark revisions" step in ci.yml (byte-identical
  post-dedent to `D203_VERIFY_REVISIONS_SCRIPT`), the root `Cargo.toml`
  `pycc_scratch` dev-dependency line, `Cargo.lock`, the D-201 blocker
  annotation, both `check_scratch_dir_usage.py` ALLOWLIST comment
  rewordings, and the ROADMAP quality-gates sentence (written against the
  post-#801 ROADMAP text if #801 merges first). Until PR-2 merges, CI
  behavior is exactly as before — the D-203 branch coexists unused.
- **PR #793 disposition** (after PR-2, under #782 not #800): rebase onto
  `main`, retarget from `feat/issue-781-scratch-dir`, drop the hunks the
  base already carries, keep the `src/main.rs`/`src/project_config.rs`
  migrations, lower ALLOWLIST counts; also fold in `f9231e2f`'s
  `tests/issue_769_optional_narrowing.rs` allowlist debt.
- **Issue #803** (duplicate D-201/D-202 renumbering) remains open and
  separate; nothing here touches the duplicate files.

## Where to resume

Read the plan comment on #800 (§4 PR-2 for the activation checklist, §7
for gates and CI expectations, §8 for risks). PR-2 must branch from
post-PR-1 `main` (its checker must contain the D-203 branch), restate the
full D-021 preflight, and verify ci.yml-to-constant byte identity locally
with `ruby scripts/check_roadmap_evidence.rb` before pushing.
