# 2026-09-04-01 -- Issue #795 merged; autopilot resumes on v0.4

## Status: #904 merged, #795 closed, autopilot paused mid-loop

`origin/main` is `01bf9447` ("Reject the PEP 654 constructs CPython rejects
in `except*` (#795) (#904)"), squashed from branch
`autopilot/iter-2026-09-03-02` at head `4a21a4a6`.
[#795](https://github.com/rotnov/pycc/issues/795) is closed by that merge.

This snapshot supersedes
[2026-09-03-02-issue-795-except-star-context.md](2026-09-03-02-issue-795-except-star-context.md),
which shipped inside #904's own diff and therefore still describes the pull
request as open and unmerged. Everything it records about *what changed* is
accurate; only its status line is stale, and per D-130 a previous
checkpoint's file is never edited to say otherwise.

## What closed out in this checkpoint

The pull request was recovered rather than authored here: three consecutive
dispatched agents died mid-task on it, and this session finished the work
directly instead of dispatching a fourth.

- **Merge conflict.** The peer session's #906 landed on `main` while #904
  sat open, conflicting in `docs/AGENT_RETROSPECTIVE.md` (both sides
  appended a dated 2026-09-03 entry -- both kept) and
  `docs/decisions/README.md` (regenerated with
  `scripts/generate_decisions_index.py`).
- **ADR-number collision.** #906 took `D-222`. This change's ADR was
  renumbered to
  [D-223](../decisions/D-223-reject-except-star-exceptiongroup-at-compile-time.md),
  rewriting only this change's own references and leaving the peer's D-222
  citations untouched.
- **Three Codex review threads**, all replied to and resolved:
  - P1, `stmt.rs` decomposition -- fixed, see below;
  - P2, `except* ExceptionGroup` subclasses -- already fixed on the branch
    (`c0001_except_star_exception_group_subclass` pins the MRO path);
  - P2, `TYPE_CHECKING`-folded bodies skipping the new checks -- declined
    and tracked as
    [#905](https://github.com/rotnov/pycc/issues/905), because a folded body
    is never lowered at all and the fix belongs with #798's rework of the
    fold.

## The P1 decomposition finding, and what it did *not* achieve

`crates/pycc_hir/src/stmt.rs` was 1,055 lines on the merge base and 1,228
with this change's additions, against AGENTS.md's ~1,000-line
decomposability threshold. `ExceptStarCtx` -- the enum, its
`shielded_by_loop` impl, and the module-doc paragraphs explaining CPython's
asymmetric `return` vs `break`/`continue` rules -- moved into
`crates/pycc_hir/src/stmt/exception.rs`, which already owns `except*`-clause
lowering. `stmt.rs` re-exports the type, so `func.rs`, `class.rs`,
`module.rs` and `stmt/for_loop.rs` resolve `stmt::ExceptStarCtx` unchanged,
and keeps a one-line pointer in its own module doc for the parameter
`lower_stmt` still threads.

`stmt.rs` is **1,148 lines** after the move -- still over the threshold.
AGENTS.md requires decomposing the part a task touches, which this did; it
does not require getting the file under the threshold, and the residual
lines are `lower_stmt`'s own match arms. There is **no D-185 tracking issue
for `stmt.rs`** (only [#548](https://github.com/rotnov/pycc/issues/548) for
`class.rs`), and this run filed none: D-192's non-milestone ceiling is in
force at 69 open non-milestone issues against a cap of 20. A future session
that wants one must first drain that count.

## Paused autopilot

- **Directive scope:** open-ended -- the standing `fix all opened issues`
  directive names no single milestone, so the loop re-enters
  `next-milestone`'s step 1 when a milestone completes.
- **Active milestone:** v0.4.
- **Last iteration's outcome:** #795 implemented and merged as #904.
- **Exact next step:** re-enter `issue-select` from `01bf9447`, v0.4 scope.
- **In-run denylist:** empty.
- **Peer-session claims to avoid:** the concurrent session owns the #881
  multi-file-import series (#898 landed via #906); its remaining open parts
  from the #881--#895 sweep should not be selected by this session.

## Known follow-ups opened by this work

- [#903](https://github.com/rotnov/pycc/issues/903) (v0.4) -- raise a real
  runtime `TypeError` for `except* ExceptionGroup`, which is what would let
  D-223 be superseded rather than narrowed further.
- [#905](https://github.com/rotnov/pycc/issues/905) -- validate
  `TYPE_CHECKING`-folded bodies, coupled to #798.
