- Record a choice that is hard to reverse as a new file under `docs/decisions/`
  (format: `docs/decisions/TEMPLATE.md`) — the `adr` skill is how to write one
  and how the log is maintained; its `scripts/generate-index.py` regenerates
  the index. Supersede an accepted decision with a new entry; never rewrite it.
