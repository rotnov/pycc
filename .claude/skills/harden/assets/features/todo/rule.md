- Defer work as one file per idea under `docs/todo/` (format:
  `docs/todo/TEMPLATE.md`) — the `todo` skill records an entry with the
  context to restart cold, then stops. The index is generated, never
  hand-edited; regenerate it after adding or closing an entry.
