- Write a handoff snapshot into `docs/sessions/` when a harden cycle ships an
  artefact, when a multi-commit task is finished, or when the user winds the
  session down — `.claude/skills/sessions/scripts/session-snapshot.py` drafts it, you fill the three
  narrative sections. Lessons from a failure go to `.harden/incidents/`, not here.
