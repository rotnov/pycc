# Project rules

- Detect and record mistakes you make into `errors.md` for later investigation.
- This project uses **uv**. Install dependencies with `uv add`, run with `uv run`.
  Never use `pip install` and never call the system `python3` directly —
  the system interpreter is 3.13 and this project requires 3.14.
