[project]
name = "salesreport"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.11.1",
    "pandas>=3.0.5",
]

[project.scripts]
salesreport = "salesreport.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
