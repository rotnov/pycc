{
  "skill_name": "pycc",
  "evals": [
    {
      "id": 1,
      "prompt": "Create a temporary self-contained typed Python program containing print(42), compile it with the pycc in this checkout, run both the generated binary and the pycc run path, and tell me exactly what happened.",
      "expected_output": "Verifies the current implementation, builds the workspace and runtime first, creates a unique self-contained temporary fixture and binary output, runs both supported execution paths, and reports the exact command, exit code, stdout, and stderr.",
      "runner": "build-and-run-self-created-fixture"
    },
    {
      "id": 2,
      "prompt": "Can pycc check --fix repair this type error for me right now?\n```python\nfor item in range(\"three\"):\n    print(item)\n```",
      "expected_output": "Runs the implemented check command and observes its T0021 diagnostic, distinguishes that current behavior from the planned --fix contract, explains that --fix is not parsed, and does not pretend the documented future repair command works.",
      "runner": "observe-current-check-fix-rejection"
    },
    {
      "id": 3,
      "prompt": "This typed Python program passes pycc check but pycc build panics:\n```python\ndef main() -> None:\n    value = print(42)\n\nmain()\n```\nFigure out whether it is a compiler bug, but do not post anything.",
      "expected_output": "Minimizes and reproduces the failure, confirms that check succeeds before build exits 101, identifies pycc_codegen backend lowering as the failing stage, reads D-064, classifies the exact PR-5 panic as an intentional temporary alpha boundary rather than a reportable defect, and does not offer or post pycc feedback.",
      "runner": "classify-planned-backend-boundary-without-write"
    }
  ]
}
