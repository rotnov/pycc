{
  "skill_name": "pycc",
  "evals": [
    {
      "id": 1,
      "prompt": "Create a temporary self-contained typed Python program containing print(42), compile it with the pycc in this checkout, run both the generated binary and pycc run path, and tell me exactly what happened.",
      "expected_output": "Verifies the current implementation, builds the workspace and runtime first, creates a unique self-contained temporary fixture and binary output, and reports the exact command, exit code, stdout, and stderr.",
      "evidence_must_contain": [
        "inline print(42) fixture",
        "pycc build",
        "generated binary",
        "pycc run",
        "printing 42"
      ],
      "contract": {
        "skill_must_contain": [
          "Build the workspace first",
          "Create a unique temporary directory",
          "Record the exact command, exit code, stdout, and stderr"
        ],
        "expected_output_must_contain": [
          "temporary",
          "exact command",
          "exit code",
          "stdout",
          "stderr"
        ]
      },
      "runtime": {
        "source": "print(42)\n",
        "expected_stdout": "42\n"
      },
      "runner": "build-and-run-self-created-fixture"
    },
    {
      "id": 2,
      "prompt": "Can pycc check --fix repair this type error for me right now?",
      "expected_output": "Distinguishes the planned CLI contract from current source, explains that the check subcommand is recognized but unimplemented while the planned --fix option is not currently parsed, and does not pretend the documented future command works.",
      "evidence_must_contain": [
        "check is recognized but unimplemented",
        "--fix is not parsed",
        "type checker remains a no-op"
      ],
      "contract": {
        "skill_must_contain": [
          "not-implemented error",
          "Do not claim support"
        ],
        "expected_output_must_contain": [
          "check subcommand",
          "unimplemented",
          "--fix option",
          "not currently parsed"
        ]
      },
      "runner": "observe-current-check-fix-rejection"
    },
    {
      "id": 3,
      "prompt": "This tiny typed Python program makes pycc panic:\n```python\ndef main() -> None:\n    value: int = 1\n    print(42)\n\nmain()\n```\nFigure out whether it is a compiler bug, but do not post anything.",
      "expected_output": "Minimizes and reproduces the failure, identifies the failing compiler stage, checks the owning specification and tests, and offers the consent-gated pycc-feedback workflow without posting.",
      "evidence_must_contain": [
        "exit 101",
        "annotated assignment",
        "HIR lowering",
        "robustness defect",
        "without posting"
      ],
      "contract": {
        "skill_must_contain": [
          "Reduce failures to the smallest",
          "uncaught compiler panic",
          "use `$pycc-feedback`",
          "without posting anything automatically"
        ],
        "expected_output_must_contain": [
          "Minimizes",
          "failing compiler stage",
          "consent-gated",
          "without posting"
        ]
      },
      "runner": "capture-parser-failure-without-write"
    }
  ]
}
