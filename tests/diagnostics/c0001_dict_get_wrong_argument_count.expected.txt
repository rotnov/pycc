error[C0001]: `.get()` is only supported as `dict.get(key, default)` with exactly two arguments so far, got 1
 --> tests/diagnostics/c0001_dict_get_wrong_argument_count.py:2:5
  |
2 | y = x.get("a")
  |     ^^^^^^^^^^ `.get()` is only supported as `dict.get(key, default)` with exactly two arguments so far, got 1
