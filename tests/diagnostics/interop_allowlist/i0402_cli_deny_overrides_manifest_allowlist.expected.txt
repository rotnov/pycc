error[I0402]: `import pprint` is a CPython-backed import, which the `deny` interop policy (set by `--interop-policy deny`) rejects: it admits no CPython import
 --> tests/diagnostics/interop_allowlist/i0402_allowlist_unlisted_root.py:1:1
  |
1 | import pprint
  | ^^^^^^^^^^^^^ `import pprint` is a CPython-backed import, which the `deny` interop policy (set by `--interop-policy deny`) rejects: it admits no CPython import
