error[T0053]: the `...` type argument in `tuple[...]` is not supported yet -- a homogeneous-variadic container has no compile-time length, so write an explicit fixed-arity annotation such as `tuple[int, int]` instead
 --> tests/diagnostics/t0053_tuple_variadic_ellipsis.py:1:10
  |
1 | def f(t: tuple[int, ...]) -> None:
  |          ^^^^^^^^^^^^^^^ the `...` type argument in `tuple[...]` is not supported yet -- a homogeneous-variadic container has no compile-time length, so write an explicit fixed-arity annotation such as `tuple[int, int]` instead
