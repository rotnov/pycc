# Recurrence: own-change-falsifies-adjacent-prose (issue #898 batch)

**Date:** 2026-09-03
**Topic:** own-change-falsifies-adjacent-prose (fifth file — counter at 5)
**Verdict:** build nothing — unchanged from the counter-3 and counter-4
escalation checks; every member is an assertion about a *neighbouring
sentence's* truth, which no gate reads, and all five were caught pre-merge by
the review tier at zero production cost
**Batch:** `.harden/findings/issue-898.jsonl`, findings 1, 2, 3 and 5
(doc-drift), rounds 1 and 2, plus one further member found in round 6

## Symptom

Five pieces of prose in the #898 change asserted a state the same change
falsified:

- `crates/pycc_hir/src/module.rs` and `crates/pycc_hir/src/hir_module.rs` —
  doc comments still attributed the top-level walk and the `HirModule` field
  population to `lower_all` after the branch moved them into `lower_module`;
- `crates/pycc_types/src/exception.rs` — a cross-crate doc reference named a
  function this branch had renamed;
- `docs/CLI_SPEC.md` — the project-imports section omitted the discovery
  fallback the same change introduced;
- round 6 — `poisonable_names`' own doc comment still asserted that a stdlib
  import "yields nothing on purpose", the exact invariant rounds 3 and 5
  deliberately overturned one screen below it.

## Root cause

Unchanged from `2026-08-22-issue-727.md`: the change's working list is derived
from what the task is *about*, and prose that merely restates the changed
behaviour from a neighbouring vantage point is in none of it. The round-6
member sharpens the class by one degree — the falsified sentence was inside
the very function being edited, and was itself edited in the same diff for a
*different* clause. Proximity is not protection.

## Termination point

Unchanged: review tier. No static rung reads an English sentence for
consistency with the code beside it.

## Disposition

All five fixed on the branch before merge.
