# Recurrence: own-change-falsifies-adjacent-prose (issue #869 batch)

**Date:** 2026-09-02
**Topic:** own-change-falsifies-adjacent-prose (fourth file — counter at 4)
**Verdict:** build nothing — unchanged from the counter-3 escalation check;
review tier remains the terminal catching rung for prose whose truth depends
on an antecedent, and the class was caught pre-merge at zero production cost
**Batch:** `.harden/findings/issue-869.jsonl`, finding 1 (doc-drift), round 1

## Symptom

The #869 change inserted a sentence about the `build.rs` diverged-root
precedent into `docs/CLI_SPEC.md` immediately before an existing sentence
that opened "It is no longer an ordinary first-build message". The inserted
sentence captured the pronoun: "It" now read as the `build.rs` warning
rather than the missing-`pycc_rt` diagnostic the paragraph is about. Fixed
on the branch (`1d0f2d5f`) by naming the referent ("The diagnostic").

## Root cause

Family match with the third entry's #784 member, which was the same shape
in the same file: an insertion into `docs/CLI_SPEC.md` that left a pronoun
in adjacent prose pointing at the wrong antecedent. The change's own
sentence was correct; the sentence it was placed next to became false.
Semantic, not mechanical — no gate resolves an English pronoun.

## Escalation check

Counter 3 already disqualified a textual artefact and walked the remaining
rungs (precommit/static: nothing reads Markdown prose for meaning;
review-check: already the catching rung, every time). Nothing in this
occurrence changes that analysis: the D-068 pinned reviewer's doc-drift
item found it in round 1, the fix was a one-word edit, and no static
pattern distinguishes a captured pronoun from a correct one. The counter
is the artefact.

## Host journal

`docs/AGENT_RETROSPECTIVE.md`'s admission bar (a mistake that cost
meaningful time, with an actionable lesson) was walked, not skipped: a
note-level one-word fix applied in the review loop fails it. No
retrospective entry for this batch.

**fixture:** none — no artefact to prove; the class has no behavioural rung
an arena can exercise
**artifact:** none (build nothing, recorded)
**verify:** `verify: manual` — the finding was fixed in `1d0f2d5f` on the
task branch before this pass; sweep of the #869 diff for sentence-initial
"It" following an inserted sentence found only the corrected site
