# Recurrence: own-change-falsifies-adjacent-prose (issue #785 batch)

**Date:** 2026-08-29
**Topic:** own-change-falsifies-adjacent-prose (third file — counter at 3)
**Verdict:** build nothing — textual artefact disqualified by the ≥3 counter
*and* already rejected at this topic's first entry; escalation ladder walked
and no mechanical rung exists for the class, so review-tier is confirmed as
the terminal catching rung rather than a rung awaiting promotion
**Batch:** `.harden/findings/issue-785.jsonl`, 2 findings, 1 review round

## Symptom

Both round-1 findings on the #785 docs-only diff, note severity, fixed in the
single amended branch commit:

- `docs/TESTING.md` — "pre-Part-1/-3" read as two discrete cohorts and
  omitted the pre-Part-2 cohort the sentence covers; reworded to
  "pre-Parts 1–3".
- `docs/DELIVERY_PLAN.md` — present-tense "#779 is closed:" asserted a
  tracker state that only becomes true when the pull request merges;
  reworded to "#779 is closed by the pull request delivering Part 5:".

## Root cause

Family match with both prior entries: prose asserting a fact — a count/range
of cohorts recorded elsewhere, a tracker state achieved only at merge — that
the tree at commit time does not support. Semantic, not mechanical; the
pinned review loop caught both, pre-merge, at zero production cost.

## Escalation check (required at counter 3)

`references/rule-audit.md` disqualifies a textual artefact at three files in
one topic, and the #727 entry had already rejected one at recurrence 1
("the rule exists; it was not executed"). The remaining rungs were examined
rather than assumed:

- **precommit/static:** no gate reads an English sentence; the topic's one
  static candidate (`cargo doc --document-private-items`, blocked on #687)
  never reaches Markdown prose. A grep for `#\d+ is closed` cannot
  distinguish premature tense from a legitimate post-merge historical
  statement (session logs assert exactly that, truthfully), so it would be a
  noise generator, not a guard — and under AGENTS.md's D-192 filing bar a
  checker for a note-level phrasing class that has never caused an incorrect
  merge decision is not tracked work.
- **review-check:** the class is already caught here, every time — the D-068
  pinned reviewer's doc-drift checklist item found all seven members across
  the three batches. There is no cheaper rung to promote to; the review tier
  *is* the detection.

Conclusion: the ladder tops out at the rung already in place. Recording the
counter is the artefact.

## Host journal

`docs/AGENT_RETROSPECTIVE.md`'s admission bar was walked, not skipped: it
requires a mistake that "cost meaningful time" with an actionable lesson.
Two note-level wording fixes applied in minutes, first try, fail that bar —
no retrospective entry for this batch.

**fixture:** none — no artefact to prove; the class has no behavioural rung
an arena can exercise
**artifact:** none (build nothing, recorded); the topic's blocked static
rung stays on #687 and is unaffected
**verify:** `verify: manual` — both findings fixed in the same commit as the
change that introduced them; sweep of the #785 diff for the two defect
shapes (`is closed` present-tense claims, `pre-Part-N/-M` cohort
compressions) found only the corrected sentences

## Postscript (after the batch pass)

An external round 2 (Codex reviewer bot, on the opened pull request)
returned 3 further findings — a fail-open canonical shell block, an
over-broad manual-cleanup command, and a Unix-only recipe presented as
platform-neutral. All three were fixed pre-merge and appended to the
findings file. They are a different class (guidance correctness, not
prose asserting unsupported facts) and arrived after this task's one
batch pass ran; per the one-pass rule they stand as data for a future
batch, not as members of this incident.
