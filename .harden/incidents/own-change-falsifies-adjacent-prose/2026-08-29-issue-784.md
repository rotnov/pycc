# Recurrence: own-change-falsifies-adjacent-prose (issue #784 batch)

**Date:** 2026-08-29
**Topic:** own-change-falsifies-adjacent-prose (second file — counter at 2)
**Verdict:** pending (counter seed; below the ≥3 bar for disqualifying a
textual artefact, and no artefact proposed at this occurrence)

## Symptom

One finding from `.harden/findings/issue-784.jsonl` (round 1, warning):
a new paragraph was inserted into `docs/CLI_SPEC.md` between the sentence
"Named D-072 boundaries include, each recognizable by its exact message."
and the bulleted list that sentence introduces, splitting an intro from its
list; the paragraph's own opening pronoun ("that scratch directory") also
depended on an antecedent buried in an earlier paragraph. Fixed on the
branch (`3df0ffac`) by relocating the paragraph to the end of the section
with self-contained wording.

## Root cause

Family match with this topic's first entry (issue #727): the edit
discharged its duty at the edit site and did not re-read the surrounding
prose as the reader will encounter it — there the adjacent prose went
stale, here the insertion point itself damaged an adjacent structure.
Semantic, not mechanical; the pinned review loop caught it.

## Fixture / artifact / verify

None — threshold for an artefact not met; review-tier confirmed as the
catching rung. A third member of this family should escalate per
`references/rule-audit.md`'s recurrence check.
