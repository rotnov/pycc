---
id: extraction-splits-one-code-arms-test-set/2026-09-11-issue-695
date: 2026-09-11
project: pycc
session: 6aebf4b1
trigger: self-post-failure
model: claude-opus-5
effort: high
harness: claude-code
type: cohesion
termination: none
related: [doc-comment-overclaims-unqualified-scope/2026-09-11-issue-695]
fixture: none — build nothing (singleton, first occurrence)
artifact: none — counter only
verify: manual — measured the `crates` tree for a static gate before declining to build one (numbers below)
verdict: pending
---

# Incident: extraction-splits-one-code-arms-test-set (counter)

**Batch:** `.harden/findings/issue-695.jsonl`, row 3 (round 3, `cohesion`,
`fixed` in `c00ad0d7`)

A five-way test-module split divided the tests covering one code arm across
two child modules. Each child then carried its own section banner citing the
same source region, so neither file's banner was false and nothing downstream
disagreed — the defect is only visible by comparing the two siblings. The fix
moved the stray test verbatim under the sibling's existing banner and dropped
the duplicate.

**Why nothing was built.** Intra-batch class size 1 (`warning`, not
`blocker`), and `ls .harden/incidents/` shows no topic for this shape across
49 folders — the batch threshold (size >= 2, or a journal match) is not met, so
this entry is the counter seed the spam guard exists to produce.

Independently of the threshold, the static rung was measured and found
infeasible for this class:

- Line-qualified section banners (`// -- ... (line N) ---`) across `crates`:
  **5 total, 0 duplicate line numbers within a directory.** The tree already
  passes a "one banner owns one cited region" rule, so the gate would ship
  with no violator it has ever caught — ceremony, not evidence.
- All section banners: **173 total, 138 distinct texts.** A "no duplicate
  banner text across siblings" rule fails three legitimate pre-existing sites
  in frozen code (a parent/child pair of one test tree plus two cross-crate
  duplicates), none of which this task may repair.

**Proposal recorded in place of approval** (host runs unattended under D-127,
no-questions law): build nothing for this class now; if a second occurrence
lands in this folder, the next pass escalates directly to the authoring-time
pointer rather than repeating the measurement.

**Cross-link:** consolidating the two banners here is what produced the
dropped qualifier recorded in
`doc-comment-overclaims-unqualified-scope/2026-09-11-issue-695`.
