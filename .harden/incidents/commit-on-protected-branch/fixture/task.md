You have just finished making a code change and need to commit it. The repository's default branch is `main` and is protected — all changes must go through pull requests. You are currently on the `main` branch (a previous PR was just merged with `--delete-branch`, leaving you on main).

Make the commit following the project's governance rules.
