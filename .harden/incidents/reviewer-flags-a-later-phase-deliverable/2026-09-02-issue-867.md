---
id: reviewer-flags-a-later-phase-deliverable/2026-09-02-issue-867
date: 2026-09-02
project: pycc
session: 2c68147a
trigger: self-post-failure
model: claude-fable-5-1
effort: medium
harness: claude-code
type: process
termination: Local-skill
related: [reviewer-flags-a-later-phase-deliverable/2026-09-02-issue-866]
fixture: none — prose extension of a dispatch-brief template; no behaviour fixture built
artifact: .claude/skills/issue-implement/SKILL.md
verify: pending — the next issue-implement pile is the measurement
verdict: pending
---

# Incident: reviewer-flags-a-later-phase-deliverable (recurrence 2)

**Batch:** `.harden/findings/issue-867.jsonl`, row 4 (`refuted`)

## What happened

This is the measurement the first entry (same day, #866) said would come. The
half of its brief sentence about step-6 deliverables held: no such finding.
The tool-scope half did not: the reviewer (Read and Grep, no `git`) reported
that it could not verify the pure-move commit was behaviour-neutral. The
orchestrator refuted it with a line-set diff of the moved code. Right about
its own limits, wrong that the limit is a finding.

## Why it was not caught

**Gap type: content.** The 2026-09-02 sentence enumerated only "gate results
or GitHub state" as claims the session verifies; a `git`-only check is a
third kind it did not name.

## Artefact and why this type

The same sentence in `.claude/skills/issue-implement/SKILL.md` step 5 now
names claims that need `git` (behaviour-neutrality of a pure move, line-set
identity of a rename, ancestry) as verified by the session, and tells the
brief to state which such claims it has already verified. Same rung as the
first entry (brief content, documentation tier); the class is caught at
review tier at zero cost, and no static command distinguishes "reviewer
cannot verify" from "missing". Not folded into
`reviewer-hypothesis-refuted-on-verification`: that topic is a wrong
prediction about the tree.

## Proof

Pending. Next pile: zero refuted rows whose note reduces to "reviewer lacks
`git`".
