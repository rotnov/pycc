---
id: reviewer-flags-a-later-phase-deliverable/2026-09-02-issue-868
date: 2026-09-02
project: pycc
session: 2c68147a
trigger: self-post-failure
model: claude-fable-5-1
effort: medium
harness: claude-code
type: process
termination: Local-skill
related: [reviewer-flags-a-later-phase-deliverable/2026-09-02-issue-866, reviewer-flags-a-later-phase-deliverable/2026-09-02-issue-867]
fixture: none — a brief template plus a pointer in a skill step; the loop that consumes it needs the pinned reviewer, so no arena fixture can exercise it (same precedent as the two earlier entries)
artifact: .claude/skills/issue-implement/references/review-brief.md
verify: pending — the next issue-implement pile is the measurement (see Proof)
verdict: pending
---

# Incident: reviewer-flags-a-later-phase-deliverable (recurrence 3, third file)

**Batch:** `.harden/findings/issue-868.jsonl`, row 1 (`refuted`).

## What happened

Round 1 of the #868 review flagged the missing `docs/sessions/` file and the
missing `.harden/findings/` pile as completeness gaps of the diff. Both are
written after the loop (the pile cannot exist before the first round's
verdicts). Refuted at zero cost, one verification step spent.

## Why it was not caught

**Gap type: compliance, with one content gap.** The step-5 sentence planted
by the #866 entry already excluded the `docs/sessions/` file; the #868
round-1 brief was typed freehand and carried the `git`/GitHub exclusions the
#867 entry added but not the step-6 deliverables — the rule existed and the
brief omitted it. The pile itself was never in the sentence (content gap).
Three files in this topic: a textual rule that has failed three times is
disqualified from a fourth rewording.

## Artefact and why this type

One rung up from a sentence the orchestrator must remember: a brief
*template* (`references/review-brief.md`) whose fixed paragraphs carry every
exclusion — session file, pull-request body, findings pile, gate/GitHub/`git`
claims — and whose slots are the only thing the orchestrator types. Step 5
now composes the brief from it. `system` tier: the exclusions travel with the
file, not with memory. Reach is this skill only, which is the only dispatcher
of that reviewer. Not a mechanical gate: no static command can tell "expected
later" from "missing", and the class is caught at review tier at zero cost.

## Proof

Pending. Measurement is the next `issue-implement` pile: zero refuted rows
about a step-6 deliverable, the findings pile, or the reviewer's own tool
scope. A fourth occurrence means the template was not used, which is a
compliance failure of a different shape (a brief that does not quote the
template) and gets its own trace.
