---
id: reviewer-flags-a-later-phase-deliverable/2026-09-10-issue-25
date: 2026-09-10
project: pycc
session: 2c68147a
trigger: reviewer-finding
model: claude-fable-5-1
effort: low
harness: claude-code
type: process
termination: none — compliance, counter only
related: [reviewer-flags-a-later-phase-deliverable/2026-09-04-issue-918, reviewer-flags-a-later-phase-deliverable/2026-09-02-issue-868]
fixture: none — no artefact was built
artifact: none — build nothing, deliberately
verify: n/a. Nothing was shipped for this class.
verdict: pending
---

# Incident: reviewer-flags-a-later-phase-deliverable (recurrence 5, fifth file)

**Batch:** `.harden/findings/issue-25.jsonl`, row 2 (`refuted`).

## Symptom

The D-068 review of a two-line docs fix flagged the absent `docs/sessions/`
handoff file as a P3 convention gap. Step 6 writes that file after the review
loop; refuted at the cost of one verification step.

## Root cause

**Gap type: compliance, same discriminator as recurrence 4.** The orchestrating
session composed the reviewer brief freehand — it never opened
`.claude/skills/issue-implement/references/review-brief.md`, whose fixed
paragraphs carry exactly the exclusion the reviewer tripped over.
`SKILL.md`'s step-5 pointer ("do not retype the exclusions from memory") was
in the loaded skill text and was skipped, this time by a session that had
already recorded recurrence 4 six days earlier.

## Termination point

Unchanged: the step-5 pointer at the template. Correct, unambiguous, not
followed.

## Artefact

**None — build nothing, deliberately**, on the same two grounds as recurrence
4: five files disqualify any further textual rung, and a compliance gap is not
addressable by text. The mechanical rung stays as recorded there — a generator
that emits the brief from the template so the fixed paragraphs are never
retyped. It is still not started here: the class costs one verification step
per occurrence and never an incorrect merge, so it fails AGENTS.md's filing
bar for process observations. What this recurrence adds is the observation
that the counter now grows within a single long session, which the generator
would also stop; the session-local mitigation taken is a persistent
agent-memory note pointing at the template, outside the repository.

## Fixture

None — no artefact to test.

## Verify

`verify: n/a`. Nothing was shipped for this class.
