---
id: history-dependent-tests-in-shallow-discovery/2026-09-05-issue-565
date: 2026-09-05
project: pycc
session: issue-565
trigger: scanner
model: unknown
effort: unknown
harness: codex
type: verification
termination: none
related: []
fixture: none
artifact: none
verify: manual
verdict: zero — build nothing additional; the fix's tests are the guard
---

## What happened

Row 6 of `.harden/findings/issue-565.jsonl` is explicitly blocker-severity:
general `test_*.py` discovery included tests needing historical blobs absent
from governance's depth-1 checkout. Batch count: 1; prior matching topic
files: 0. Severity alone triggers the full ladder, not a pending singleton.
All repair work is already in `8474efb4`.

## Why it was not caught

Inline severity trace: reproducing discovery in its actual checkout depth
would have caught the missing-blob prerequisite before review. The fresh
batched tracer places prevention at test-harness ownership, after the plan's
workflow/CI-equivalent audit requirements. Gap type: content in test
environment coverage, not a missing branch-protection rule. No historical
tool sequence is invented, and no skipped workflow step is asserted.
The gate-sweep-omission topic is not a match: this test ran in an incompatible
environment rather than a required command being omitted.

## Artefact and why this type

The fix's own tests are the guard — build nothing additional. The full-history
suite is outside generic discovery and is explicitly invoked by the existing
fail-fast Pages harness. `scripts/test_site_execution_wiring.py` binds that
suite/harness/Pages/history boundary with four tests: clean wiring, missing
suite invocation, missing Pages harness invocation and depth-1 Pages checkout.
This static guard reaches both agent platforms; no per-harness reminder or
arena is needed. Changing governance checkout policy would cross a separately
owned policy seam and was rejected by independent review during repair.

Proposal: retain the wired tests and add this record only. No workflow,
permission, threshold, settings or required-check change. The host postmortem
admission check finds a repaired test-execution defect but no separately
established process mistake; it does not manufacture a retrospective entry.

## Proof

Fresh `python3 -m unittest discover -s scripts -p
'test_site_execution_wiring.py' -v`: 4 tests passed. The negative tests reject
each broken wiring condition; the healthy binding passes. The full Pages
mutation harness also exercises the suite through its intended owner.
The implementation snapshot separately records fresh depth-1 discovery:
991 tests, six skips, successful. The preliminary reproduction included an
expected-message assertion failure as well as the genuine missing-blob
failure; this record does not relabel that entire run as a clean A/B proof.

Sweep result: 0 pre-existing violations found in the execution suite's
discovery name, shell invocation and Pages full-history binding. This is a
bounded wiring sweep, not a claim that every repository test is history-free.
Decomposition: no new rules; the four shallow-safe tests already form the
cohesive ownership check.
