---
id: test-seam-widens-public-api/2026-09-11-issue-1006
date: 2026-09-11
project: pycc
session: 2c68147a
trigger: reviewer-finding
model: claude-fable-5-1
effort: high
harness: claude-code
type: convention
termination: none
related: [test-seam-widens-public-api/incident]
fixture: none
artifact: none
verify: manual — fix commit `8e5adcf5` cited in the pile
verdict: pending
---

# Incident: test-seam-widens-public-api (file 2, #1006)

**Batch:** `.harden/findings/issue-1006.jsonl`, row 3 (`fixed` in `8e5adcf5`).

## What happened

The validator test suite's `assert_rejected` helper carried a `verify_git`
flag that no call site set; the flag was consumed inside the helper body, so
it was live code with zero external consumers. `ievo:deep-reviewer` round 2
flagged it; the flag was removed because `GitObjectTests` already exercises
`verify_git` directly.

## Why it was not caught

Same root as the topic's first file (a Rust `pub` seam with only in-crate
consumers): a seam written for an anticipated caller that never
materialised. The parameter was used inside the helper, so no unused-argument
lint fires, and Python has no visibility lint at all. Gap type: convention.
Intra-batch count: 1. Prior topic files: 1 — this is the second occurrence,
cross-language (a Python parameter instead of a Rust `pub`).

## Artefact and why this type

Build nothing, second counter. No mechanical check distinguishes "parameter
no caller sets" from a defaulted parameter one caller will set next week;
the reviewer's dead-code checklist item is the rung that caught it and the
cost was one round. Per `references/rule-audit.md` a third occurrence
escalates; the candidate then is a reviewer-brief line, not a static gate.

## Proof

`verify: manual` — the pile row and its fix commit.
