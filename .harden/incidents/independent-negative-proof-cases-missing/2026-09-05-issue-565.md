---
id: independent-negative-proof-cases-missing/2026-09-05-issue-565
date: 2026-09-05
project: pycc
session: issue-565
trigger: scanner
model: unknown
effort: unknown
harness: codex
type: verification
termination: none
related: []
fixture: none
artifact: none
verify: manual
verdict: zero — build nothing additional; the fix's tests are the guard
---

## What happened

Rows 3 and 7 of `.harden/findings/issue-565.jsonl` share an independence gap:
the initial mutation matrix omitted promised cases, and page-wide links
allowed footer content to satisfy a primary-navigation obligation. Batch
count: 2; prior matching topic files: 0. Both were fixed in `8474efb4`.
The wrong-guard journal topic is not a match: it concerns expected failures
passing on an unrelated diagnostic, not invalid navigation being accepted.

## Why it was not caught

The single read-only batched trace terminates at the implementation/test
validation seam: existing success/failure/edge requirements and the plan's
independent matrix preceded an incomplete test/checker implementation.
Gap type: content in the initial test layer. The plan already required
independence; the available evidence does not establish a missing agent rule
or the original agent's reason for omitting cases.

## Artefact and why this type

The repair already promoted the observed cases from review to static tests.
`scripts/site_execution_evidence_test.py` exercises nested fields, each
artifact, immutable-blob co-mutations, ordered outputs, JSON semantics and
independent projections. Its navigation test removes each new route from
each of seven primary navigations while leaving footers intact: 14 mutants.
`scripts/check_site_evidence.py` validates that navigation independently.
The shared test runner checks a nonzero status and the expected diagnostic.

Proposal: build nothing additional; retain those tests, add this class record,
and replace/consolidate no prose. A generic future-schema completeness guard
was not built and is not claimed. An external skill, hook or behavioral arena
would not improve this already executable oracle. The host postmortem
admission check routes these ordinary test/code defects to their tests,
not to a separate process narrative.

## Proof

The full Pages mutation harness tests the clean tree and deliberate violators,
including all 14 footer-masked navigation cases, at the unchanged
implementation `1ff7dfe5`. This is static proof, not an arena result or a
new harden artifact.

Sweep result: 0 pre-existing violations found across the seven canonical
primary navigations and the two accepted execution records checked by the
public harness. Decomposition: no new rules; the shared execution test and
validator seams already contain the checks.
