# Incident: doc-comment-overclaims-unqualified-scope (recurrence 3)

**Date:** 2026-09-02
**Topic:** doc-comment-overclaims-unqualified-scope
**Verdict:** build nothing — counter only; the instance predates the task
**Batch:** `.harden/findings/issue-712.jsonl`, finding 1 (doc-drift), round 1

## Symptom

While reviewing the count-free rewording of `.github/workflows/ci.yml`'s
conformance-test comments (issue #712), the reviewer read the adjacent
`tests/conformance.rs` and found that the comment above
`pep_0594_dead_battery_rejected_produces_c0001` says the test runs by
default "unlike every other test in this file". Two oracle-free helper unit
tests in the same file are also not `#[ignore]`d, so the "every other"
qualifier is over-broad. The diff under review did not touch that file, and
its own wording ("oracle-backed tests are `#[ignore]`d") is exact.

## Root cause

Same shape as the 2026-08-29 and earlier 2026-09-02 entries: prose accurate
for the cases the author had in mind (the oracle tests), stated without the
qualifier that bounds it (the helper unit tests). Nothing mechanical reads
an English qualifier in a `//` comment.

## Termination point

The 2026-09-02 recurrence-2 entry said a third recurrence re-opens the
question of an explicit "qualify every justification" line in the
implementer brief. Answered inline: this instance was authored long before
this task (with the D-138 PEP 594 test) and surfaced only because the
reviewer read an adjacent untouched file, so an implementer-brief line
would not have prevented it, and the review tier caught it at zero cost
exactly as the prior entries predicted. Build nothing; counter recorded.
The overclaim itself is out of this pull request's scope (the #712 plan
excludes `tests/conformance.rs`) and is left for a follow-up issue.

**fixture:** none — build nothing
**artifact:** none — counter only
**verify:** n/a — no fix in this pull request
