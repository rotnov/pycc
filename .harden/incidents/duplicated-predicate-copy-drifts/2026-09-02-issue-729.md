---
id: duplicated-predicate-copy-drifts/2026-09-02-issue-729
date: 2026-09-02
project: pycc
session: 6aebf4b1
trigger: batch
model: claude-fable-5-1
effort: medium
harness: claude-code
type: code
termination: none
related: []
fixture: none — singleton counter seed
artifact: none — the fix's own test (`harness_modules_on_disk_ignores_directories_and_non_rs_files`) is the guard
verify: manual — the ScratchDir test plants a directory named `fake.rs` and asserts it is not returned
verdict: pending
---

# Incident: duplicated-predicate-copy-drifts (first file)

**Batch:** `.harden/findings/issue-729.jsonl`, row 2 (round 1, cross-file-consistency).

## What happened

One filesystem predicate ("a harness cohort is a regular `*.rs` file directly
under the directory") was implemented three times — a Rust helper, a Python
checker and a control test — and the third copy, written from memory, dropped
the `is_file()` half.

## Why it was not caught

No test ties the three copies together; each is unit-tested alone. The
reviewer's cross-file consistency check caught it in round 1.

## Artefact and why this type

Below the batch threshold (size 1, no journal topic): recorded as an open
counter. If it recurs, the candidate rung is a shared single-owner helper that
the other readers call, or a sync test that feeds the same fixture tree to all
copies.

## Proof

None beyond the fix's own test; open counter.
