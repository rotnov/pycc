---
id: duplicated-predicate-copy-drifts/2026-09-10-issue-80
date: 2026-09-10
project: pycc
session: 2c68147a
trigger: reviewer-finding
model: claude-fable-5-1
effort: low
harness: claude-code
type: verification
termination: none
related: [duplicated-predicate-copy-drifts/2026-09-02-issue-729, plan-contradicts-its-own-constraint/2026-09-10-issue-80]
fixture: none
artifact: none
verify: manual — the fix commits are cited in the findings pile; no guard built
verdict: pending
---

# Incident: duplicated-predicate-copy-drifts (recurrence, file 2)

**Batch:** `.harden/findings/issue-80.jsonl`, rows 1 and 3 (both `fixed`).

## Symptom

The vendored-skill lock defines its payload as "the tracked entries of this
repository's index under the skill prefix". The change wrote that authority
three times: the production enumerator (which resolved the git toplevel from
the skill directory, so a nested `.git` inside the skill root redirected
`ls-files` to the nested index), the round-1 test helper (correct — calls the
production enumerator), and the round-2 test fixture (a working-tree `rglob`
walk behind a hand-maintained two-pattern ignore list, so an untracked stray
file in the real skill directory would have been hashed and broken the accept
test plus every mutation test routed through it).

## Root cause

The published plan itself specified both proxies: "anchored on the skill root
rather than on `root`" for the enumerator and `shutil.copytree` for the
fixture, while the same plan forbids walking the working tree for the lock.
Gap type: content (plan) — the plan's adversarial review loop did not check the
plan against its own constraint. Batch count: 2. Prior topic files: 1; this is
file 2.

## Termination point

`.claude/skills/issue-to-plan/SKILL.md`, adversarial-review step: every helper
or fixture a plan specifies must derive from the same authority the production
spec names. The code-level termination is the shape `765861c7` already built —
one owner (`skill_payload_entries`) that both the lock and the fixture call.

## Artefact

Build nothing: the fix's own end-to-end nested-boundary tests and the
single-owner helper are the guard for this instance, and the plan-level seam is
counted under the new topic `plan-contradicts-its-own-constraint`. Sweep
result: `grep -n "rglob\|show-toplevel" scripts/validate_agent_assets.py
scripts/test_validate_agent_assets.py` after `765861c7` shows no remaining
working-tree walk feeding a lock digest.

## Fixture

None.

## Verify

`verify: manual` — the round-1 and round-2 findings were reproduced by reading
the enumerator and the fixture; fix commits `6c5b98d2` and `765861c7` are in
the pile.
