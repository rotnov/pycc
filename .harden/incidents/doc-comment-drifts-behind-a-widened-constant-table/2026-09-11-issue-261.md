---
id: doc-comment-drifts-behind-a-widened-constant-table/2026-09-11-issue-261
date: 2026-09-11
project: pycc
session: 6aebf4b1
trigger: reviewer-finding
model: claude-opus-5
effort: high
harness: claude-code
type: test
termination: none
related: [doc-comment-drifts-behind-a-widened-constant-table/2026-09-10-issue-260, doc-comment-drifts-behind-a-widened-constant-table/2026-08-23-issue-739, own-change-falsifies-adjacent-prose/2026-08-22-issue-727]
fixture: none — no artefact shipped; the fix's own shape is the guard
artifact: none — counter only
verify: manual — one-site sweep, recorded below
verdict: build-nothing
---

# Incident: doc-comment-drifts-behind-a-widened-constant-table (file 3)

**Batch:** `.harden/findings/issue-261.jsonl`, row 2 (`fixed` in `cff79ba5`).
Class size 1 within the batch; crosses the threshold on the journal match.

## Symptom

A mutation test selected its subject out of a shared contract tuple by positional
slice and asserted a hard-coded length beside it. It was correct when written and
mis-scoped the moment the tuple grew — and the growth arrived inside the same review
round, from the batch's own first fix, which added one more pinned phrase.

## Root cause

The seed topic's predicate exactly: a derived count restated as a literal has no
mechanical link to its source, so it is correct on the day it is written and wrong
the first time the source widens. The carrier is new — in the two prior files it was
prose, here it is test-selection code, which fails silently by narrowing what the
test covers rather than by stating something false.

Gap type: absence. Batch count: 1. Prior topic files: 2.

## Termination point

None — the fix's own shape removes the coupling permanently at that site: a named
sub-tuple, concatenated into the shared constant, selected by name, with tail
membership asserted instead of a literal count.

## Artefact

**None**, and this one was settled by measurement rather than by assumption. File 2
of this topic escalated to a static gate (`validate_alpha_skill_count_prose`) with
`verdict: profit`, which made "extend a proven gate one carrier over" a live
candidate, so the sites were counted: across the two test modules that touch the
seven contract tuples, positional-slice or hard-coded-length coupling has exactly one
instance — the line this round's own fix rewrote, which is now structurally immune.
A gate with one instance, already immune, has nothing left to catch.

Sweep result: 1 site, 0 remaining after the fix.

## Fixture

None.

## Verify

`verify: manual` — the one-site sweep above.
