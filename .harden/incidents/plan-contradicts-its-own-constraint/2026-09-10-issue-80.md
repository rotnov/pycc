---
id: plan-contradicts-its-own-constraint/2026-09-10-issue-80
date: 2026-09-10
project: pycc
session: 2c68147a
trigger: reviewer-finding
model: claude-fable-5-1
effort: low
harness: claude-code
type: verification
termination: none
related: [duplicated-predicate-copy-drifts/2026-09-10-issue-80, doc-comment-drifts-behind-a-widened-constant-table/incident]
fixture: none
artifact: none
verify: manual — the fix commits are cited in the findings pile; no guard built
verdict: pending
---

# Incident: plan-contradicts-its-own-constraint (counter seed, file 1)

**Batch:** `.harden/findings/issue-80.jsonl`, rows 1, 3 and 4 (all `fixed`).

## Symptom

Three review findings against a change that implemented its published plan
faithfully. The plan stated one constraint — the lock is defined over the
repository's tracked index, never a working-tree walk — and then, in its own
work items, specified an enumerator anchored on the skill directory (row 1), a
`shutil.copytree` fixture (row 3), and a documentation class list that was a
subset of the check order the same plan mandates for the code (row 4). Each
work item contradicted the plan's own stated constraint, and the plan's
adversarial review loop (five rounds, ending clean) did not catch any of them.

## Root cause

The plan's review loop verified the plan against the tree and the issue, not
the plan against itself: a constraint stated in one section and a work item
that violates it in another were never read together. Gap type: content
(plan). Batch count: 3 (all from one plan). Prior topic files: 0; this is the
seed.

## Termination point

`.claude/skills/issue-to-plan/SKILL.md`, step 7 (adversarial review loop): the
reviewer's brief should include "for every constraint the plan states, name
the work item that satisfies it and check no work item violates it". Whether
that is a prose addition or a checklist item is the next batch's call.

## Artefact

None yet — `verdict: pending`, a counter seed. A row-4 mechanical candidate
is recorded for the next occurrence: `validate_skill_lock` already runs a
field-presence loop over `docs/AGENT_TOOLING.md`, so asserting that each
rejection-reason constant is named in the documented paragraph is the same
shape and would make that sub-class static-tier.

## Fixture

None.

## Verify

`verify: manual` — the tracer read the plan comment
(https://github.com/rotnov/pycc/issues/80#issuecomment-5622746014) §3 line 30,
§4 items 1 and 3, against `.harden/findings/issue-80.jsonl` rows 1, 3, 4.
