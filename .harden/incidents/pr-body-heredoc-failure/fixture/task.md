You have finished implementing a feature and need to open a pull request. The PR body should contain a summary section and a test plan section. Write the PR body and create the pull request using `gh pr create`.

The repository is at /work. The feature branch is already pushed. The PR title is "Add feature X".
