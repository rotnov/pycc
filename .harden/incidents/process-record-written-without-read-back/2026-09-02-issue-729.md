---
id: process-record-written-without-read-back/2026-09-02-issue-729
date: 2026-09-02
project: pycc
session: 6aebf4b1
trigger: batch
model: claude-fable-5-1
effort: medium
harness: claude-code
type: process
termination: precommit
related: [process-record-written-without-read-back/incident]
fixture: scripts/test_check_harden_findings.py (unchanged) — its real-repository case is the reproduction
artifact: scripts/check_harden_findings.py (unchanged) — the existing gate is the guard; nothing built
verify: manual — the first three pile rows (round 1-2) failed `test_real_repository_piles_conform` locally with "missing key(s) file, category, summary, disposition, note"; after the rewrite the checker and the test pass, and CI runs the same test via `python3 -B -m unittest discover -s scripts` (.github/workflows/ci.yml:87)
verdict: profit
---

# Incident: process-record-written-without-read-back (recurrence 1, second file)

**Batch:** `.harden/findings/issue-729.jsonl` — a process finding not in the pile.

## What happened

The pile's first three rows were written in an invented schema
(`severity/source/status/title/detail/resolution`) instead of the one
`references/batch.md` shows. Nothing read them back until the full local
`scripts` unittest run failed on the real-repository case.

## Why it was not caught

The writer composed a plausible shape from memory instead of opening the
schema example. The guard from this topic's original incident fired exactly
where it was placed — at read-back, before the pull request — which is the
positive evidence its `verdict: profit` claimed.

## Artefact and why this type

Build nothing. The existing checker and its CI-run test are the right rung
and reach every harness; a second textual reminder would be the weaker
artefact the ladder rejects. The batch tracer's suggestion that the checker
is "not wired into CI" was refuted against `.github/workflows/ci.yml:87`.

## Proof

Sweep result: `python3 scripts/check_harden_findings.py .harden/findings/issue-729.jsonl`
exits 0 and `scripts.test_check_harden_findings` passes on the rewritten pile.
