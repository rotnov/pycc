{"round": 1, "file": "tests/issue_977_instance_string_conversion.rs", "category": "test-coverage", "summary": "Three D-237 repro-table shapes (non-dataclass subclass of a dataclass, plain generic instance, print(self)) had no public-CLI pinning test", "disposition": "fixed", "fix_commit": "a862b063", "note": "Added three assert_c0001 tests; each verified to report the D-237 message naming the class."}
{"round": 1, "file": "crates/pycc_types/src/module.rs", "category": "methodology", "summary": "Reviewer had no shell, so the diff boundary and the shadowing-shape stderr wording were reconstructed from source rather than an executed git diff / cargo test", "disposition": "refuted", "note": "Not a defect in the change: the diff boundary was confirmed by the dispatching session with git diff --stat f8e9d2e3..HEAD, and cargo test --workspace, the 23 public-CLI tests and the 28 unit tests all pass on the reviewed head."}
{"round": 2, "file": "crates/pycc_types/src/monomorphize.rs", "category": "correctness", "summary": "Codex P1 (PR #985): a generic @dataclass specialization was built with is_dataclass=false, so check accepted print(Box[int](1)) while build rejected the 0gen_ specialization with C0001", "disposition": "fixed", "fix_commit": "a78c1662", "note": "Reproduced with genb.py (check exit 0, run C0001); at origin/main e77b4b13 the same program passed check and panicked in codegen. The specialization now carries the origin's is_dataclass and substituted dataclass_fields; pinned by unit tests on check_and_resolve and a public-CLI build-and-run test printing Box(n=1)."}
{"round": 2, "file": "crates/pycc_types/src/string_conversion.rs", "category": "diagnostic-quality", "summary": "Codex P2 (PR #985): the help recommended @dataclass for a class under a builtin exception name, which the predicate rejects by name regardless", "disposition": "fixed", "fix_commit": "a78c1662", "note": "Builtin-named classes now get a rename remedy; pinned by two unit tests and a public-CLI JSON test on a print(self) shape (a call to the shadowing class is reported by the solver's own message under D-220, so that shape cannot surface the help)."}
