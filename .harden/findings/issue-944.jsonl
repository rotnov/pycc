{"round": 1, "file": "crates/pycc_hir/src/module.rs", "category": "doc-drift", "summary": "Module doc and D-233 decision 6 claimed a cascade-silenced item emits no diagnostic of any kind, but the #944 scan extends diagnostics unconditionally after every item, so such an item can still report a true enum-call C0001.", "disposition": "fixed", "fix_commit": "d7d18c48", "note": "Narrowed both texts to the lowering source only (as D-233 decision 3 already does); the scan is deliberately not filtered on the cascade outcome."}
{"round": 1, "file": "crates/pycc_types/src/class/binding.rs", "category": "doc-drift", "summary": "The guard's reachability comment said it is reached through the scan's documented over-suppression cases in general; only the position-insensitive cases reach it, while binding-form shadows yield T0021 elsewhere.", "disposition": "fixed", "fix_commit": "d7d18c48", "note": "Narrowed the clause to limit (i)'s two position-insensitive cases and its 'or T0021' framing."}
{"round": 1, "file": "docs/decisions/D-233-hir-lowering-gains-a-second-per-item-diagnostic-source.md", "category": "test-coverage", "summary": "Decision 5 claimed every residual limit is pinned by a test, but the comprehension-target-in-same-def and call-before-later-module-rebinding residuals had no dedicated test.", "disposition": "fixed", "fix_commit": "d7d18c48", "note": "Added the two unit tests in crates/pycc_hir/src/class/enum_call.rs so decision 5 is literally true."}
{"round": 2, "file": "crates/pycc_hir/src/class/enum_call.rs", "category": "correctness", "summary": "PR #971 Codex review (P1): the scan walked the original AST, so a call inside an if/elif TYPE_CHECKING body that lower_stmt constant-folds away (#790, D-223) was reported as an enum-call C0001 and rejected a module the lowering accepts.", "disposition": "fixed", "fix_commit": "3416241c", "note": "Both the call scan and the frame binder now route every if through walk_if_as_lowered, skipping guarded bodies (calls and bindings) with the same is_type_checking_guard predicate and import view the fold uses; the pre-loop module frame keeps an aliased t.TYPE_CHECKING residual (limit (vi), over-suppression only). Nine unit tests pin the shapes."}
{"round": 3, "file": "crates/pycc_hir/src/module.rs", "category": "efficiency", "summary": "PR #971 frontend-perf-gate: the per-item enum-call scan walked every item (and each def body again for its frame) even when no enum class was known, costing the pycc check frontend bench 7-10% against the exact predecessor (threshold 7%).", "disposition": "fixed", "fix_commit": "a5a6fb5b", "note": "Reproduced locally (main 8.11 us vs branch 8.73 us median, +7.6%); lower_module now builds the name set as borrowed &str and calls the scan only when it is non-empty; local bench after the fix: 8.28 us, +1.8%."}
{"round": 4, "file": "crates/pycc_hir/src/module.rs", "category": "correctness", "summary": "PR #971 Codex P2: a module-level def Color() preceding Color() and class Color(Enum) made the scan report a false-kind enum-call C0001 ahead of the class/function collision diagnostic, because the syntactic pre-collection knew the name before its class item and the frame binder records no def name.", "disposition": "fixed", "fix_commit": "191d1a9d", "note": "Reproduced with pycc check in all three orderings; lower_module now drops every module-level def name from the scan name set (limit (vii)), so the collision diagnostic is reported alone; 4 unit tests + 1 end-to-end test."}
{"round": 5, "file": "crates/pycc_hir/src/module.rs", "category": "correctness", "summary": "PR #971 Codex P2 (second round): the def-only filter still let an ordinary class Color, a from m import Color, or a type Color alias preceding Color() and class Color(Enum) report a false-kind enum-call C0001 ahead of the collision diagnostic.", "disposition": "fixed", "fix_commit": "da819a4d", "note": "Reproduced with pycc check for the ordinary class and the project import; module_rebound_names now drops every name bound by two or more module-level def/class/import/type statements; +2 unit tests, +1 end-to-end test, +1 helper test."}
{"round": 5, "file": "docs/DIAGNOSTICS.md", "category": "doc-drift", "summary": "PR #971 Codex P2: DIAGNOSTICS.md promised the enum-call C0001 at the call expression categorically although the residual shapes D-233 enumerates (module-level rebinding anywhere in the module, sibling-comprehension rebinding) fall back to the span-less guard at 1:1.", "disposition": "fixed", "fix_commit": "da819a4d", "note": "The sentence now names the residual fallback."}
{"round": 6, "file": "crates/pycc_hir/src/class/enum_call.rs", "category": "correctness", "summary": "PR #971 Codex P2: module_rebound_names counts a same-named def that fails to lower (unannotated `def Color()`, then `class Color(Enum)`, then `Color()`), so the call is never scanned and no collision fires either; Codex proposed outcome-based suppression.", "disposition": "refuted", "note": "Accepted D-219 consequence (its documented `class A:` / `def A()` / `def g(a: A)` example): the root-cause T0001 is always reported and fixing it surfaces the collision on the next run. Outcome-based suppression would emit a false-kind C0001 at a `Color()` that lexically resolves to the def, the wrong-kind report limit (vii) exists to prevent. Independent advisor round recorded in the session file."}
{"round": 7, "file": "crates/pycc_hir/src/class/enum_call.rs", "category": "correctness", "summary": "PR #971 Codex P2: module_rebound_names counted an identical repeated import (`from colors import Color` twice) as a rebinding, so the call was never scanned and fell to the span-less backstop at 1:1 although lower_module accepts the repeat as binding the same class twice.", "disposition": "fixed", "fix_commit": "c7c77dde", "note": "Bindings now carry their imported definition; an import repeating an earlier (local, source) pair is not a rebinding. Unit test over identical/differing repeats, end-to-end test asserting the call span."}
{"round": 7, "file": "crates/pycc_hir/src/class/enum_call.rs", "category": "maintainability", "summary": "PR #971 Codex P1: enum_call.rs reached 1,013 lines, over AGENTS.md's ~1,000-line decomposition threshold.", "disposition": "fixed", "fix_commit": "c7c77dde", "note": "The 547-line inline test module moved to the sibling enum_call_tests.rs (path attribute, still a child module); the scanner is 488 lines."}
{"round": 8, "file": "crates/pycc_hir/src/class/enum_call.rs", "category": "correctness", "summary": "PR #971 Codex P2: one enum imported under one local name through two module paths (`from colors import Color` beside `from palette import Color`, palette re-exporting it) enters rebound_names, so the call falls to the span-less guard at 1:1; Codex proposed comparing the resolved ImportBinding::Project origin.", "disposition": "refuted", "note": "The 1:1 fallback reproduces, but the proposed mechanism does not exist: ImportBinding::Project.module_path is the module the name was imported from (`palette`), and copy_class_with_ancestors clones a re-exported class without any defining-module tag, so HIR carries no provenance to compare. The shape is recorded as one more D-233 residual reported by the backstop (limit (vii), decision 5, DIAGNOSTICS.md); provenance tracking is a separate feature outside #944."}
{"round": 9, "file": "docs/DIAGNOSTICS.md", "category": "doc-drift", "summary": "PR #971 Codex P2: DIAGNOSTICS.md's quality-bar bullet, docs/CLI_SPEC.md's check contract, and .claude/skills/pycc/SKILL.md still said HIR lowering reports one diagnostic per failing top-level item, although D-233's per-item scan appends one C0001 per enum-class call after the item's own diagnostic.", "disposition": "fixed", "fix_commit": "8dfe739c", "note": "All three now describe the two per-item sources; the .agents/ Codex mirror is a pointer to the Claude skill and needed no edit."}
{"round": 10, "file": "docs/CLI_SPEC.md", "category": "doc-drift", "summary": "PR #971 Codex P2: CLI_SPEC.md still promised the first diagnostic byte-stable across releases although D-233 decision 4 lets a scanned C0001 precede a later item's own diagnostic.", "disposition": "fixed", "fix_commit": "78d8a626", "note": "Now described as the completed #864 transition invariant (D-217 rule 2), matching DIAGNOSTICS.md."}
{"round": 10, "file": "docs/CLI_SPEC.md", "category": "doc-drift", "summary": "PR #971 Codex P2: CLI_SPEC.md's report-order section and src/frontend.rs's Frontend error doc still limited HIR output to failing top-level items.", "disposition": "fixed", "fix_commit": "78d8a626", "note": "Both now name the per-item pair: the item's own diagnostic, then its enum-call C0001s (D-233)."}
{"round": 10, "file": "docs/DIAGNOSTICS.md", "category": "doc-drift", "summary": "PR #971 Codex P2: the D-219 cascade sentence said a silenced item carries no diagnostic of any kind, although the D-233 scan still runs over it and reports a call to another valid enum class (decision 6).", "disposition": "fixed", "fix_commit": "78d8a626", "note": "Silence scoped to the lowering diagnostic; new unit test pins the scan report on a cascade-silenced def."}
{"round": 11, "file": "docs/CLI_SPEC.md", "category": "doc-drift", "summary": "PR #971 Codex P2: the per-item count contract promised one enum-call C0001 per call unconditionally, although a scan-suppressed name (shadowed, rebound, or under another D-233 limit) is left to the span-less type-checker guard.", "disposition": "fixed", "fix_commit": "5b12e7b8", "note": "CLI_SPEC.md, DIAGNOSTICS.md, src/frontend.rs and the pycc skill now count only calls the scan can attribute and name the 1:1 fallback."}
{"round": 12, "file": "crates/pycc_hir/src/class/enum_call.rs", "category": "over-suppression", "summary": "PR #971 Codex P2: module_rebound_names counted a from __future__ import as a binding, so an enum sharing a feature name (class annotations(Enum)) lost its call span for no collision.", "disposition": "fixed", "fix_commit": "f674edce", "note": "Future imports are skipped (lower_future_import binds nothing); unit and CLI tests pin the call-span report; a relative .__future__ import still counts."}
{"round": 12, "file": "crates/pycc_hir/src/module.rs", "category": "performance", "summary": "PR #971 Codex P2: module_bindings walked every live module-level statement unconditionally, so a module with no enum class still paid an AST traversal despite D-233 decision 1.", "disposition": "fixed", "fix_commit": "f674edce", "note": "The module frame is now built on the first scanned item against the pre-loop import slice (state.imports is append-only); a new unit test keeps the name-less except handler branch covered in that lazily built frame."}
{"round": 12, "file": "docs/decisions/D-233-hir-lowering-gains-a-second-per-item-diagnostic-source.md", "category": "test-coverage", "summary": "PR #971 Codex P2: D-233 claimed every enumerated residual is pinned by a test, but the two-path re-export import residual had no fixture.", "disposition": "fixed", "fix_commit": "f674edce", "note": "tests/issue_921_enum_call.rs now asserts the colors/palette two-path import reaches the span-less guard at 1:1 and is not claimed by the scan."}
{"round": 13, "file": "crates/pycc_hir/src/class/enum_call.rs", "category": "correctness", "summary": "PR #971 Codex P2: the scope binder skipped a nested definition whole, so a walrus in a decorator, type parameter, default, annotation, return annotation, or class base did not bind the enclosing frame and a later module-level call produced a false-kind enum-call C0001 outside a class body.", "disposition": "fixed", "fix_commit": "a093c901", "note": "Binder::visit_stmt now walks a FunctionDef's decorators, type params, parameters, and return annotation and a ClassDef's decorators, type params, and arguments into the enclosing frame while still skipping the body; limit (iii) and D-233 document the residual over-suppression under the def's own frame; eight unit tests pin the shapes."}
{"round": 14, "file": "crates/pycc_hir/src/module.rs", "category": "doc-drift", "summary": "PR #971 Codex P2: lower_all's rustdoc still promised a C0001 for every enum-class call, while a scan residual lowers Ok and is caught by the type checker's span-less guard.", "disposition": "fixed", "fix_commit": "7cb92306", "note": "Qualified to calls the scan can attribute, matching CLI_SPEC/DIAGNOSTICS."}
{"round": 14, "file": "crates/pycc_hir/src/module.rs", "category": "doc-drift", "summary": "PR #971 Codex P2: lower_module's contract still said the first collected diagnostic is byte-identical to the pre-#867 result, contradicting D-233 decision 4 where an earlier item's scan diagnostic may precede a later item's lowering failure.", "disposition": "fixed", "fix_commit": "7cb92306", "note": "The guarantee is now stated for the first lowering diagnostic, with the scan-first ordering called out."}
{"round": 15, "file": "crates/pycc_hir/src/module.rs", "category": "doc-drift", "summary": "PR #971 Codex P2: lower_module's rustdoc said a cascade-suppressed item is skipped with no diagnostic of any kind, while the post-item enum-call scan still reports a call to another valid enum class inside it (D-233 decision 3).", "disposition": "fixed", "fix_commit": "ae2b064c", "note": "Silence is now scoped to the item's own lowering diagnostic, with the scan called out."}
{"round": 15, "file": "docs/ROADMAP.md", "category": "doc-drift", "summary": "PR #971 Codex P2: the roadmap's #944 entry and docs/TYPE_SYSTEM.md's current-state sentences claimed the C0001 is reported at the call expression categorically, while documented scan residuals fall back to the span-less guard at 1:1.", "disposition": "fixed", "fix_commit": "ae2b064c", "note": "Both qualified to calls the scan can attribute, naming the 1:1 fallback."}
