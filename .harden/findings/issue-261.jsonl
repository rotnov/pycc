{"round": 1, "file": ".claude/skills/issue-to-plan/SKILL.md", "line": 220, "category": "completeness-gap", "summary": "step 7's clean-round and impasse definitions are not disjoint -- a round whose only finding is a recurring one resolved as \"considered, no change\" satisfies both -- and the precedence between them lived only in plan_review_terminal_state's arm order and a test comment, never in the canonical text", "disposition": "fixed", "fix_commit": "cff79ba5", "note": "Raised by the D-068 pinned reviewer (iEvo deep-reviewer) at warning severity. In scope rather than a scope expansion: acceptance criterion 1 of #261 is literally \"define one unambiguous clean-round condition\", so an ambiguity reintroduced by the fix is the same defect in a new place. Fixed by stating the precedence in step 7 immediately after the impasse definition, and pinning that sentence as a fifth ISSUE_TO_PLAN_CONTRACT substring so it cannot be deleted with the gates green. Arm order verified directly: the clean arm precedes the round-count arm, so a fifth round ending in no change is clean, and the gap the sentence closes is exactly one arm wide."}
{"round": 1, "file": "scripts/test_run_alpha_skill_evals.py", "line": 502, "category": "test-fragility", "summary": "the per-phrase mutation test selected the loop contract phrases with the positional slice tuple(ISSUE_TO_PLAN_CONTRACT)[3:] plus a hard-coded len == 4 assertion, so adding a fifth pinned phrase silently moves the boundary", "disposition": "fixed", "fix_commit": "cff79ba5", "note": "Raised by the pinned reviewer at note severity, and made load-bearing by the round's own first fix, which adds exactly that fifth phrase. Fixed by naming ISSUE_TO_PLAN_LOOP_CONTRACT in run_alpha_skill_evals.py and concatenating it into ISSUE_TO_PLAN_CONTRACT; the test now selects by name and asserts the named tuple is that constant's tail instead of a hard-coded count. Verified nothing else reads the constant by identity, order, or length."}
