{"round": 1, "file": "crates/pycc_hir/src/class/reserved_names.rs", "category": "correctness", "summary": "shape-only _is_private port under-rejects `_C__x = 1` inside `class C(Enum)`; CPython matches the raw dict key against the literal `_<ClassName>__` prefix, so a source name that was never mangled is still kept out of the member list", "disposition": "fixed", "note": "confirmed against CPython 3.13.9 (members=['B'] for `_C__x`, members=['_D__x','B'] for the same spelling in `class D`); added a fourth, class-name-keyed arm and gave ClassBodyRoute::Enum the enclosing class name", "fix_commit": "aefab117"}
{"round": 1, "file": "crates/pycc_hir/src/class/reserved_names.rs", "category": "doc-accuracy", "summary": "ENUM_DUNDER_MESSAGE claimed every dunder is left as an ordinary class attribute; `__order__` is not", "disposition": "fixed", "note": "confirmed against CPython 3.13.9: `_EnumDict` rewrites the key to `_order_` and `EnumType.__new__` pops it out of the class dict entirely (and `__order__ = 1` raises TypeError: 'int' object is not iterable); message and D-238 corrected", "fix_commit": "aefab117"}
{"round": 1, "file": "crates/pycc_hir/src/class/reserved_names.rs", "category": "doc-accuracy", "summary": "the predicate was documented as over-rejecting in exactly two families; a third exists (one leading underscore, two or more trailing)", "disposition": "fixed", "note": "confirmed against CPython 3.13.9: `_x__` and `_foo___` are ordinary members because `_is_sunder` also requires name[-2] != '_'; documented in reserved_names.rs, D-238 and docs/TYPE_SYSTEM.md", "fix_commit": "aefab117"}
{"round": 1, "file": "docs/TYPE_SYSTEM.md", "category": "doc-drift", "summary": "the reserved-name bullet still said `Two independent sets live there` after this change added a third", "disposition": "fixed", "note": "now `Three independent sets live there`, with a third sub-bullet describing the route-gated, class-name-keyed `_EnumDict` set", "fix_commit": "aefab117"}
{"round": 2, "file": "crates/pycc_hir/src/tests/enum_non_member_names.rs", "category": "test-coverage", "summary": "two documented over-rejection families (`__`/`___`/`____`/`___x___` and `_x__`/`_foo___`) had no behavioral pin, so a later narrowing toward CPython's exact shape would flip them silently", "disposition": "fixed", "note": "added `the_shape_only_over_rejections_are_pinned`; D-014's gate measures branch reachability, not outcome, so it would not have caught the flip", "fix_commit": "aefab117"}
{"round": 3, "file": "crates/pycc_hir/src/class/reserved_names.rs", "category": "doc-accuracy", "summary": "ENUM_PRIVATE_MESSAGE claimed CPython keeps `__x` out of the member list, which is false when the enclosing class name itself begins with an underscore", "disposition": "fixed", "note": "raised by chatgpt-codex-connector on PR #988 and confirmed on CPython 3.13.9: `class _C(Enum): __x = 1` beside `B = 2` gives ['_C__x','B'] because mangling strips the class name's leading underscores while _is_private does not. The rejection stands (modelling it needs a name-mangling pass pycc lacks, else the member's .name reads back wrong); the message now states both CPython outcomes and D-238 records the fourth over-rejected family", "fix_commit": "af2d0afd"}
