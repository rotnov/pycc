{"round": 1, "file": "crates/pycc_types/src/tests.rs", "line": 15, "category": "doc-drift", "summary": "the `#934` comment above the `mod` block names only the protocol-return split, so the newly inserted `mod constraints;` sits under a comment that does not describe it", "disposition": "fixed", "fix_commit": "b0431981", "note": "Raised by the pinned reviewer as an explicit non-finding (the ambiguity pre-dates this diff: import_alias/init_rank/protocol_argument already sat under the same comment). Recorded and fixed anyway because inserting `mod constraints;` directly beneath it made the mismatch adjacent rather than merely nearby; the comment was generalized to name the rule plus both issue numbers. No other site restates that comment: `grep -rn '#934' crates/` returns 1 hit, the line itself."}
{"round": 2, "file": "crates/pycc_types/src/tests.rs", "line": 518, "category": "doc-drift", "summary": "extracting the constraint tests left two comment runs behind in the parent, where they re-bound to unrelated adjacent tests, and the moved tests lost the headers that described them", "disposition": "fixed", "fix_commit": "5f2b1bba", "note": "Raised post-CI by the optional `chatgpt-codex-connector` reviewer (P2), not by the pinned reviewer. Both runs moved into `crates/pycc_types/src/tests/constraints.rs` above the tests they describe. Same class as round 1 and the #677 topic, but deletion-caused rather than insertion-caused; recorded as a fourth occurrence in `.harden/incidents/insertion-retargets-a-doc-comment-run/2026-09-07-b.md`."}
{"round": 3, "file": "crates/pycc_types/src/tests/generic_method_instantiation.rs", "line": 405, "category": "cohesion", "summary": "the three tests covering `is_assignable`'s `Ty::Param` clause were split across two child modules, each carrying its own banner citing the same source region (line 3229)", "disposition": "fixed", "fix_commit": "c00ad0d7", "note": "Raised by the pinned reviewer (warning) on the five-way generic-monomorphization split. One code arm's coverage landed in two cohesion-driven children, which is exactly the cohesion property the split exists to establish. `is_assignable_param_clause_accepts_scalar_assignment_in_generic_method` moved verbatim into `tests/generic_method_dedup.rs` under that file's existing `// -- is_assignable Ty::Param branch (line 3229)` banner, and the duplicate banner was dropped. Both module doc headers were updated so neither claims coverage it no longer holds. A new sub-class for this issue: the prior two rounds were doc-comment-run retargeting, this one is a cut that divided a single code arm's test set."}
{"round": 4, "file": "crates/pycc_types/src/tests/generic_class_instantiation.rs", "line": 6, "category": "doc-drift", "summary": "the new module doc header claimed unqualified that the file holds the #387 tests driving the full check_and_resolve -> monomorphize -> instantiate_generic_class_methods pipeline, but its last two tests stop at `check` and never reach that pipeline", "disposition": "fixed", "fix_commit": "5447e0f5", "note": "Raised by the pinned reviewer (note) in round 2. Third occurrence of this issue's dominant class, but a new sub-shape: rounds 1 and 2 were prose left behind by a move re-binding to the wrong tests, this is prose newly written for a child that overstates what the child holds. The header now names the two `check`-only coverage tests explicitly."}
{"round": 4, "file": "crates/pycc_types/src/tests/generic_method_dedup.rs", "line": 7, "category": "doc-drift", "summary": "the round-3 fix's own wording, \"every test covering the `is_assignable` `Ty::Param` clause\", omitted the (line 3229) qualifier its own banner carries and so read as covering the distinct `from == Ty::Param` clause at line 3240, which lives in `tests/generic_monomorphization_arms.rs`", "disposition": "fixed", "fix_commit": "5447e0f5", "note": "Raised by the pinned reviewer (note) in round 2, against the round-3 fix rather than against the original extraction. Worth recording separately: a fix that narrows a doc claim can itself overclaim when it drops the qualifier the adjacent banner already carries. The header now names both the line-3229 clause it owns and the line-3240 clause it does not."}
